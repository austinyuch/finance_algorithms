# RTM.md — Requirements Traceability Matrix (Bridge)

> Cross-spec traceability and verification context only. **Not** a readiness
> authority — authoritative Live-Demo Readiness lives in each
> `.agents/specs/**/review.md`; the stable registry is
> [`SPECS.md`](./SPECS.md); rolling state is [`NEXT_STEPS.md`](./NEXT_STEPS.md).
> Generated docs ([`docs/manual/`](../../docs/manual/),
> [`docs/review/`](../../docs/review/)) consume this bridge for verification
> context, never to derive verdicts.

## Epic → requirement → implementation → test → review

| Epic | Core requirement (abridged) | Key implementation | Test / evidence anchor | Review verdict |
|---|---|---|---|---|
| A0 | Lookahead-safe vectorized/low-frequency event replay backtest with OOS-net metrics, walk-forward, parallel run, tracking | `quantlab/{contracts,data,engine,parallel,tracking}` | `test_a0_0..5`; CR-A0 regime scheduling; CR-A0 event replay; current mutation spot checks 123/123 configured/killed | **PASSED** |
| A | TSMC hedge slice ranked vs baselines; optional PyTorch LSTM lane isolated from default runtime | `quantlab/strategies/`, `scripts/run_tsmc_hedge_slice.py`, `pyproject.toml`, `uv.lock` | `test_a_1,3,4,5`; optional `test_a_2_lstm`; `test_dependency_security.py`; `root-torch-default-dependency`, `demo-tsmc-store-close-gate`, `demo-vintage-store-close-gate`, and `demo-showcase-payload-tempdir-gate` mutations | **PASSED** (synthetic; `no_alpha_claim`; default env no Torch) |
| Governance | Current-state evidence freshness and false-green prevention | `tests/quantlab/test_governance_guards.py`; `scripts/local_ci_matrix.py`; `scripts/run_mutation_spot_checks.py`; `.agents/specs/governance-evidence-refresh/`; `NEXT_STEPS.md`; `f-public-static-showcase/change-requests/cr-fps-001-hosting-manifest-proof-sync.md`; `f-public-static-showcase/change-requests/cr-fps-002-hosting-content-hash-proof.md`; `f-public-static-showcase/change-requests/cr-fps-003-hosting-manifest-contract-proof.md`; `f-public-static-showcase/change-requests/cr-fps-007-public-probe-parity-status.md`; `f-public-static-showcase/change-requests/cr-fps-008-public-probe-freshness-gate.md`; `f-public-static-showcase/change-requests/cr-fps-009-dashboard-visual-readiness-wireup.md` | stale governance guard 25 passed, including current mutation-count SSOT sync, fixture-backed legacy-only evidence contract, and machine-readable local CI matrix proof; governance mutations killed, including current mutation/test registry drift and public-hosting proof regressions; Dependabot #7 fixed; current registries use default-root **478 passed, 2 skipped** evidence, H-2 torch-UAT **430 passed**, public probe `proven` / hash `matched` for deployed==expected `dataHash c33da57d…` after the 2026-06-18 `main` deploy, visual readiness, local-first CI workflow-cost policy, and visual diff `0 / 1,296,000` | **PASSED** (`governance-evidence-refresh` + promotion-boundary guard + local-first CI guard and skill-body mutation + local CI matrix guard + CR-FPS-001..CR-FPS-011) |
| B | PIT vintage loader, FRED proxies, as-of alignment, `pit_strictness`, source-health, snapshot run report + ops gate + scheduled run observer + scoped live write smoke + broad source-quorum gate + live quorum proof + Stooq fail-closed proof wrapper | `quantlab/data/`, `scripts/{daily_snapshot,snapshot_ops_gate,source_quorum_proof,stooq_contract_proof,scheduled_run_observer}.py`, `.github/workflows/daily-snapshot.yml` | `test_b_1..5`; `test_daily_snapshot.py` (42); CR-B5/B7..B20; Actions runs `27387041974` (`workflow_dispatch`) and `27392471359` (`schedule`); scheduled observation `status=proven`, `schedule_run_count=1`; CR-B12 live smoke `ok=1` then `skip=1`; CR-B19 live proof `status=proven`, `fail=0`, `skip=6` for FRED/Yahoo/NOAA quorum; CR-B20 live Stooq `spy.us` probe `status=not_proven` after HTTP 404; current-lane field stays stable `none`; promotion proof authority is GitHub PR state plus spec-local reports, not an exhaustive `NEXT_STEPS.md` ledger | **PASSED (repo-side + scoped live write smoke + workflow_dispatch + autonomous cron dry-run proof + fail-closed observer + source-quorum live proof + Stooq fail-closed proof wrapper + promotion-boundary guard)**; residual `ISSUE-B3-001` (Stooq availability); CR-B21 deep-backfill FRED rate/FX residual **closed** (idempotent re-run, manifest `fail=0`) |
| C | Optimizer, multi-period allocation, regime rebalance selector, pyramid-entry adapter | `quantlab/portfolio/` | `test_c_1..5` (123) | **PASSED** |
| D | First-regime, return/risk forecast, robust optimizer, family evaluator — OOS-net baselines | `quantlab/models/`, `quantlab/research/` | `test_d_1,3,4,5,6`; real-data regime benchmark; CR-DME-001 (evaluator accepts a claim-silent dumb baseline, rejects only explicit overclaim; `d-model-evaluation-alpha-gate` mutation killed); CR-RDO-005 multi-cycle leaderboard — regime now full-feature after CR-B21 `T10Y2Y` residual backfilled (Sharpe `0.6694`) | **PASSED** (`no_alpha_claim`) |
| A0/B/D (real-data OOS) | First honest real-data ≥2-asset OOS-net comparison over the existing engine; PIT no-lookahead, survivorship, net≠gross under cost; fail-closed `insufficient_data` | `quantlab/research/real_data_oos.py`, `scripts/run_real_data_oos_backtest.py` | `test_real_data_oos` (32); `test_real_data_oos_cli` (4); `real-data-oos-sufficiency-asset-gate` / `real-data-oos-net-sharpe-segment` / `real-data-oos-baseline-visibility` mutations killed; focused coverage 98%; evidence `real-data-oos-backtest/reports/real-data-oos-artifact.json` | **PASSED** (`no_alpha_claim`; default proxy universe not yet co-temporal — mechanism evidence, not a strategy verdict) |
| H (deep-learning research lab, H-1) | Framework-free deterministic reference MLP forecaster + A0 adapter; multi-framework backend registry (torch/jax/tf) with honest reference fallback (framework-isolation preserved); deterministic checksummed statistical performance report (OOS-net authority, distribution/rolling-Sharpe/drawdown/learning-curve); self-contained SVG/HTML viz; parameterized experiment CLI with ExperimentRegistry MLOps lineage; fail-closed `insufficient_data` | `quantlab/models/dl_forecaster.py`, `quantlab/models/dl/backends.py`, `quantlab/research/model_performance_report.py`, `quantlab/research/model_report_viz.py`, `scripts/run_dl_experiment.py` | `test_h_dl_forecaster` (10); `test_h_model_performance_report` (7); `test_h_dl_experiment_cli` (5); `h-deep-report-oos-net-ranking` / `h-deep-report-no-alpha-claim` / `h-backend-honest-fallback` mutations killed; focused H slice line coverage 95.7%; import-linter "DL backend boundary" contract KEPT | **PASSED** (`no_alpha_claim`; reference backend proves the mechanism; real torch/jax/tf training, live UI, and production Tier3 deferred) |
| H (deep-learning real training, H-2) | Real PyTorch training of the reference MLP via the backend dispatch; lazy torch confined to `quantlab/models/dl/`; float64 seed-init parity ≤`1e-3`; deterministic; optional default-skipped torch lane; honest `reference` fallback; framework-isolation preserved | `quantlab/models/dl/torch_trainer.py`, `quantlab/models/dl_forecaster.py` (additive dispatch) | `test_h2_torch_training` (4, torch lane); `test_h2_backend_isolation` (2); `h2-torch-real-training` / `h2-torch-reference-parity-seed` mutations KILLED (torch UAT); torch UAT full suite **430 passed**; import-linter KEPT; `reports/h2-torch-uat-capture.md` | **PASSED** (`no_alpha_claim`; repo-side + torch UAT; JAX/TF real backends, GPU, live UI, production Tier3 deferred) |
| H (interactive research UI, H-3) | Interactive parameter UI over H experiment/report artifacts with deterministic lineage, `no_alpha_claim`, OOS-net-only evidence display, approximate-data warnings, local-first UI/API/smoke/VRT obligations, and fail-closed public/static proof behavior | `quantlab/showcase/scenario.py`; `frontend/lib/{showcase-contract,interactive-research}.ts`; `frontend/components/InteractiveResearchPanel.tsx`; public-demo section/smoke/visual/e2e artifacts | `test_h3_interactive_showcase`; `frontend/tests/interactive-research.test.ts`; dashboard/export/smoke tests; `npm run e2e:interactive` browser fail-closed VRT; frontend mutations `frontend-h3-interactive-claim-boundary`, `frontend-h3-approximate-warning-gate`, and `frontend-h3-e2e-failclosed-status-gate`; browser visual `0 / 1,296,000`; public probe `proven` / `matched` for deployed==expected `dataHash c33da57d…` after the 2026-06-18 `main` deploy | **PASSED** (repo-side/local static replay; public Pages parity proven) |
| H (live backend rerun API, H-4) | Real Python ASGI live-rerun backend over the H-3 static-replay UI: validated-parameter recompute via `run_experiment` returning a checksummed `live_compute` artifact; Next.js proxy with honest static-replay fallback; TS lifecycle reducer (idle/computing/computed/fail_closed/error) + bounded client; `LiveRerunStatus` 5-state component; public PIT-provider read view (kills `provider._prices`, resolves `ISSUE-DDD-PROVIDER-PRIVATE-001`); charter guard (no actionable-signal surface, Lane 2 gated) | `quantlab/showcase/rerun_service.py`; `quantlab/data/provider.py` (public view); `frontend/app/api/experiment/rerun/route.ts`; `frontend/lib/live-rerun.ts`; `frontend/components/LiveRerunStatus.tsx` | `test_h4_provider_view.py` (9); `test_h4_live_rerun.py` (18, incl. `test_payload_carries_no_actionable_signal`); `test_h4_rerun_smoke.py` (3 — real-backend uvicorn dynamic-port smoke proving fresh compute + negative stub-fails-freshness, FMEA-H4-01); `frontend/tests/{live-rerun,live-rerun-status}.test.ts(x)` (18); Python mutation `h4-rerun-validation-fail-closed-gate` + repointed `a0-chaos-asset-span-usable-close-filter` KILLED | **PASSED · deployed (SDD Phase 5)** (`no_alpha_claim`; deploy-coupled residuals — static byte-parity, browser e2e/VRT, frontend-mutation/count re-pin — landed with the 2026-06-21 `main` Pages deploy at `dataHash 6c18e572…`, public hosting `proven`/`matched`) |
| E | Experiment registry, config catalog, checksum snapshot durability, registry→dashboard bridge, Tier3 readiness gate, local serving/retraining/automated drift monitoring smoke evidence, production-tier evidence gate, governed production evidence probes, strict readiness proof CLI | `quantlab/mlops/`, `quantlab/showcase/`, `scripts/tier3_readiness_gate.py` | `test_e_1` (24), `test_e_production_evidence` (13); `test_tier3_readiness_gate_cli` (5); `e-tier3-readiness-gate`, `e-tier3-gate-proof-digest`, `e-evidence-explicit-claim-boundary`, `e-serving-smoke-health-gate`, `e-retraining-smoke-status-gate`, `e-tier3-production-tier-gate`, `e-automated-drift-status-gate`, `e-production-serving-endpoint-gate`, `e-production-retraining-status-gate`, `e-production-external-proof-uri-gate`, `e-production-external-identity-uri-gate`, `e-tier3-manifest-artifact-uri-gate`, `e-tier3-experiment-binding-gate`, `e-production-retraining-artifact-uri-gate`, `e-production-artifact-scheme-allowlist-gate`, `e-production-observed-at-utc-gate`, `e-production-drift-threshold-gate`, and `e-tier3-cli-serving-validator` mutations killed; focused `experiment_registry` + `production_evidence` combined line coverage 99% | **PASSED** (local smoke only; production proof requires external payloads with traceable external HTTPS proof URLs, allowlisted production identity URI schemes, allowlisted remote artifacts, UTC observed_at timestamps, positive drift thresholds, and consistent manifest experiment binding) |
| F | Showcase read API, Next.js dashboard, demo hardening, public/static showcase, canonical local result-store payload, browser visual readiness wire-up | `quantlab/showcase/`, `frontend/` | `test_f_1`; `frontend` npm test; static export; CR-FPS-006 source metadata guard; CR-FPS-007 committed probe parity-status guard; CR-FPS-008 probe freshness guard; CR-FPS-009 visual-readiness payload guard | **CONDITIONAL** · `local_demo_only` |
| F/B/E ops | Browser visual diff, public-hosting probe, deployment manifest/probe/content-hash/contract/freshness consistency, schedule run proof, scoped/live quorum snapshot proof, E drift report | `frontend/scripts/{browser-visual-smoke,probe-public-demo}.mjs`, `frontend/scripts/export-public-demo.tsx`, `docs/{deployment-manifest,public-hosting-probe}.json`, `frontend/visual-baselines/browser-visual.png`, `.github/workflows/daily-snapshot.yml`, `scripts/{daily_snapshot,source_quorum_proof,stooq_contract_proof}.py` | `browser-visual.png` `proven`; repo-baseline pixel diff `0 / 1,296,000`; public-hosting proof is now `proven` / `matched` because deployed==expected `dataHash c33da57d…` after the 2026-06-18 `main` deploy; standalone probe records `freshnessStatus=fresh` / `maxAgeHours=24` and CR-FPS-008 guards stale observation plus missing expected-manifest overclaim; Actions `schedule` run `27392471359`; CR-B12 live write smoke; CR-B19 broad FRED/Yahoo/NOAA quorum proof; CR-B20 Stooq proof fails closed on HTTP 404; Python/frontend mutations cover public proof, stale evidence, and visual underclaim regressions | **PASSED** (ops-visual-drift-artifacts + f-browser-pixel-baseline + b-live-scheduled-snapshot-proof + promotion-boundary guard + CR-FPS-001..CR-FPS-011 + CR-B19 + CR-B20) |
| G | Source-contract-first local alt-data loader (two optional slices) | `quantlab/data/` alt-data loader | `test_g_1` (7); PBT + mutation | **PASSED** (default-disabled) |
| Legacy | Arithmetic/geometric pyramid order sizing API | `invest_algorithms/` | `tests/test_algo_pyramid.py` | stable legacy baseline |

## Verification snapshot (2026-06-18)

| Gate | Command | Result |
|---|---|---|
| Python suite | `uv run pytest -q` | **478 passed, 2 skipped** (default root env); H-2 torch-enabled UAT remains **430 passed** |
| Type check | `uv run mypy quantlab/ scripts/build_showcase_payload.py scripts/run_tsmc_hedge_slice.py scripts/run_vintage_slice.py scripts/scheduled_run_observer.py scripts/tier3_readiness_gate.py scripts/source_quorum_proof.py scripts/stooq_contract_proof.py scripts/run_real_data_oos_backtest.py --ignore-missing-imports` | clean, 69 files |
| Architecture | `uv run lint-imports` | KEPT (88 files, 242 deps) |
| Frontend unit | `cd frontend && npm test` | 70 passed |
| Frontend supply chain | `npm audit --omit=dev` | 0 vulnerabilities |
| E registry + production evidence line coverage | `uv run pytest --cov=quantlab.mlops.experiment_registry --cov=quantlab.mlops.production_evidence --cov-report=term-missing tests/quantlab/test_e_1_experiment_registry.py tests/quantlab/test_e_production_evidence.py` | 37 passed; 99% combined line coverage |
| Python mutation | `uv run python scripts/run_mutation_spot_checks.py` | 123/123 configured mutations killed, including CR-RDO-004 `real-data-oos-sampling-frequency-guard`, CR-A0 `engine-event-driven-date-gate`, `result-store-finite-oos-net-sharpe`, CR-B12 `snapshot-scoped-source-health`, CR-B19 `b-source-quorum-proof-exit-gate` / `b-source-quorum-proof-file-gate`, CR-B20 `b-stooq-proof-exit-gate` / `b-stooq-proof-file-gate`, governance `governance-stale-next-steps-alert`, `governance-stale-post-merge-sync-promotion`, `governance-stale-cron-proof-pending`, `governance-exhaustive-pr-ledger-regression`, `governance-stale-mutation-count-regression`, `governance-stale-import-linter-count-regression`, `governance-stale-import-linter-formalization-regression`, `governance-local-first-ci-default-regression`, `governance-local-first-ci-skill-default-regression`, `governance-refresh-review-stale-evidence-regression`, `governance-stale-dashboard-source-wording`, `governance-stale-f-nextjs-requirements-fixture`, `governance-stale-f-nextjs-fixture-review`, `governance-stale-visual-evidence-regression`, `browser-visual-doc-sync-gate-regression`, CR-FPS-001 `public-hosting-manifest-status-overclaim`, CR-FPS-007 `public-hosting-probe-status-overclaim` / `review-public-hosting-probe-status-overclaim` / `public-hosting-probe-hash-overclaim`, CR-FPS-002 `public-hosting-manifest-hash-overclaim`, CR-FPS-003 `public-hosting-manifest-contract-regression`, `public-hosting-taxonomy-authority-regression`, `manual-showcase-payload-sync-regression`, `frontend-showcase-payload-sync-regression`, `review-pytest-gate-transcript-regression`, `review-frontend-count-shorthand-regression`, `review-frontend-coverage-artifact-drift`, `review-audit-gate-transcript-regression`, `governance-test-registry-count-drift`, `mutation-test-registry-count-drift`, `e-evidence-explicit-claim-boundary`, `e-production-external-proof-uri-gate`, `e-production-external-identity-uri-gate`, `e-tier3-manifest-artifact-uri-gate`, `e-tier3-experiment-binding-gate`, `e-production-retraining-artifact-uri-gate`, `e-production-artifact-scheme-allowlist-gate`, `e-production-observed-at-utc-gate`, `e-production-drift-threshold-gate`, `showcase-frontend-transcript-failure-gate`, `f-showcase-visual-diff-contract-gate`, `f-showcase-public-probe-freshness-gate`, `f-showcase-public-probe-observed-at-gate`, `f-showcase-public-probe-observed-at-age-gate`, `f-showcase-retired-fixture-marker`, `governance-f-cr-superseded-fixture-boundary`, `public-hosting-probe-expected-hash-drift`, and CR-FPS-011 `public-hosting-freshness-downgrade-window` / `public-hosting-stale-downgrade-not-proven`; CR-FPS-008 is covered by the frontend mutation row |
| Frontend mutation | `cd frontend && npm run mutation` | 29/29 killed, including `frontend-h3-interactive-claim-boundary`, `frontend-h3-approximate-warning-gate`, `frontend-h3-e2e-failclosed-status-gate`, `frontend-public-demo-hosting-freshness-gate`, CR-FPS-008 `frontend-public-demo-probe-freshness-status-gate`, `frontend-public-demo-probe-absolute-output-path`, `frontend-public-demo-export-absolute-output-dir`, `frontend-public-demo-export-stale-evidence-gate`, `frontend-public-demo-expected-manifest-gate`, `frontend-public-demo-probe-manifest-colocation`, `frontend-public-demo-probe-incomplete-manifest-failclosed`, `frontend-static-export-showcase-sync`, `frontend-coverage-artifact-drift`, CR-FPS-009 `frontend-visual-regression-underclaim`, `frontend-smoke-port-hardcode-regression`, and `frontend-smoke-html-api-parity-regression` |
| Browser visual | `cd frontend && npm run visual:browser` | `proven`; repo-baseline pixel diff `0 / 1,296,000` at threshold `0.001` |
| Public-hosting probe | `npm run probe:public-demo` (or `uv run python scripts/refresh_public_hosting_proof.py --probe-json ...`) | HTTP 200 plus deployed manifest HTTP 200; expected `dataHash c33da57d…` is now deployed after the 2026-06-18 `main` deploy, so the committed proof is `proven` / hash `matched` (observed `2026-06-18T07:47Z`). CR-FPS-011 makes committed-evidence freshness deterministic (injected `asof`): a stale observation downgrades to `configured_not_observed` instead of crashing the build, and `refresh_public_hosting_proof.py` automates re-proving |

## Open verification gaps (owned elsewhere)

- No CI-managed visual baseline history beyond the committed repo baseline —
  residual in `f-browser-pixel-baseline/review.md`.
- GitHub Actions autonomous `event=schedule` dry-run proof exists
  (`27392471359`); scheduled observer artifact records `status=proven`,
  `schedule_run_count=1`. CR-B12 separately proves scoped local live
  append-only write/skip mechanics. CR-B19 proves broad FRED/Yahoo/NOAA source
  quorum for 2026-06-12. CR-B20 proves Stooq restoration attempts fail closed
  without positive finite live close rows; Stooq remains a source-contract gap.
- Stooq source contract — `ISSUE-B3-001` (folded into CR-B7/B8/B9/B20; still blocked/default-disabled).

> Resolved 2026-06-11/12: live chromium-headless browser screenshot
> (`browser-visual.png` `proven`) and public-hosting probe (HTTP 200 plus
> deployed manifest contract metadata) now exist. The H-3 payload proof on 2026-06-18
> is now `proven` / `matched` — Pages serves expected
> `dataHash c33da57d…` after the `main` deploy (`49a4510`). CR-FPS-006 now generates the dashboard payload from a
> local `LocalResultStore` / `ExperimentRegistry` scenario, and dashboard
> readiness remains conservative as `local_demo_only`.

Doc reconciliation on 2026-06-12 surfaced **no new unowned issues**; all gaps
trace to an existing spec/CR/review or `ISSUE_LOG.md`.
