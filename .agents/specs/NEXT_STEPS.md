# NEXT_STEPS.md — Rolling Operational Memo

> 高階、滾動、可覆寫。跨 spec 當前狀態與恢復點。詳細真相見各 spec artifact。

## Current State (2026-06-21)

- **`docs-enrichment-h-eda` — Implemented · Review PASSED (docs-only, branch `spec/docs-enrichment-h-eda`).** Documentation deliverable (no behavior/payload change): (1) 強化 showcase/review + manual 圖文並茂（self-contained 內嵌 SVG 圖）、(2) Epic H 深度學習探索分析 (EDA) 內容（真實 committed 數字：multi-cycle 2000–2026 Regime 0.669 > BuyAndHold 0.657 > Forecast 0.354 > Robust 0.321；SP500 BuyAndHold 0.877 > SmaTiming 0.808；H-3 DeepForecast 0.91 vs 0.63）、(3) future-work roadmap。Scope guardrail: deploy-coupled payload 凍結（`dataHash 6c18e572` proven、counts 478/70/123 不變），`no_alpha_claim` 全程，guards 維持 25。Depends-on F + Epic H specs；無 CR（純 additive）。Next: Phase 2 design + lightweight FMEA。Pointer: `.agents/specs/docs-enrichment-h-eda/{requirements,design,tasks,review}.md`.
- **2026-06-21 — H-4 live-rerun API DEPLOYED + count snapshot bumped (`dev` == `main` == `9ee9f55`).** The deploy-coupled residuals that older bullets below list as *pending/abandoned* are now **DONE** via the proper deploy-coupled flow (PR #135 payload+counts, PR #136 re-prove): regenerated `docs/showcase.json` → `dataHash 6c18e572…`, public hosting **`proven` / `matched`** (live-probed 2026-06-21T04:18Z), and the published snapshot advanced to **pytest 478 / frontend 70 / Python mutation 123/123** across every guard-coupled + stakeholder surface (`TESTS.md`, both generation guides, manual/review HTML, `FEATURES.md`, `RTM.md`, `SPECS.md`). H-4 is now **Implemented · Review PASSED · Deployed**. The standalone count bump abandoned as non-converging (PR #131) was the wrong path; coupling it to a real deploy converged — see the deploy-coupling & count-refresh policy in `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md`. Older bullets retained for history.
- **2026-06-20 promotions + deferrals (`dev` == `main` == `d7c04d0`):**
  - **H-4 live-rerun API — Implemented · Review PASSED · merged to `main` (PR #128).** Public PIT-provider view (kills `provider._prices`, resolves `ISSUE-DDD-PROVIDER-PRIVATE-001`), Python ASGI rerun backend wrapping `run_experiment`, Next.js proxy + static-replay fallback, TS lifecycle reducer, 5-state `LiveRerunStatus` component, real-backend uvicorn dynamic-port smoke (fresh-checksum + negative stub-fails-freshness, FMEA-H4-01), charter guard. See [h-live-rerun-api/review.md](./h-live-rerun-api/review.md).
  - **Mutation-gate refresh — DONE · merged to `main` (PR #129).** Fixed a latent runner false-green (pytest exit-5 "no tests collected" was counted as KILLED), repaired 21+ stale anchors + 6 stale selectors hidden by that bug (E source/tests moved to `production_evidence.py`/`test_e_production_evidence.py`), found+fixed a real test-quality gap (`f-showcase-visual-diff-contract-gate`), and **re-validated the full suite to a clean 123/123** (torch excluded via the new `--exclude`). Resolves `ISSUE-MUT-GATE-ROT-001` + `ISSUE-MUT-RUNNER-FALSEGREEN-001`.
  - **Count-payload bump — ABANDONED (PR #131 closed).** Bumping the published pytest count 435→486 + regenerating the dashboard payload is purely cosmetic and **does not converge**: regenerating `docs/showcase.json` shifts the `dataHash` (flipping hosting to `configured_not_observed` until a post-deploy live re-probe), and the same number must be rewritten across the bilingual `docs/manual/**` + `docs/review/index.html` while the Copilot PR reviewer (`required_conversation_resolution`) re-blocks each push. Demonstrated non-convergent, so deferred to the next real dashboard-data change + deploy. The deploy-coupling policy is now documented in `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md` and `AGENTS.md`.
  - **Deploy-coupled deltas — SUPERSEDED 2026-06-21 (see top entry; now pytest 478 / mutation 123 / `dataHash 6c18e572…` proven):** the prior published / guard-coupled snapshot — `docs/showcase.json` + manual/review HTML + `quantlab/TESTS.md` + both generation guides — is pytest **435 passed** and Python mutation **118 published**, with public hosting **`proven` / `matched`** for `dataHash c33da57d…` (live-probed 2026-06-18T07:47Z via `scripts/refresh_public_hosting_proof.py --live`; the dashboard self-claim stays `not_proven` by static-artifact contract). The **governance SoT runs the mutation count ahead at 123/123** (`TESTS.md`/`RTM.md`; the 5 chaos/CR additions are not cross-guarded into the HTML payload, so that delta is expected). The H-4 + mutation-refresh merges added Python tests, so the **actual** full suite now exceeds the published 435 — but it was **not** re-captured end-to-end this session (Hypothesis-heavy, >9 min), so 435 stays the guard-coupled value until the next dashboard-data deploy bumps every count surface together. Regenerating the payload for a standalone count bump is the **abandoned PR #131 path** (it shifts the `dataHash`, flipping hosting to `configured_not_observed` until a post-deploy live re-probe) — see the deploy-coupling policy in `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md` + `AGENTS.md`.
- **2026-06-19 spec-master gap pass + integration (merged to `dev`):** A codebase gap/tech-debt/known-issue pass (test taxonomy, DDD/refactor, live-rerun architecture) produced two chaos-hardening CRs, an issue-log batch, and the H-4 requirements draft, all integrated into `dev` via `--no-ff` merges.
  - **CR-A0-CHAOS-001 (Implemented, repo-side):** [a0 chaos CR](./a0-backtest-foundation/change-requests/cr-a0-chaos-001-invalid-market-data-hardening.md) — chaos pass caught **two real latent bugs**: (1) `VectorizedEngine._close` had no finite/positive guard and `_simulate`'s `if p0 and p1` treats `NaN` as truthy, so a NaN/negative close silently fabricated NaN/bogus OOS-net metrics → now treated as a missing leg; (2) `compute_metrics` produced `NaN` `max_drawdown` on a total-loss path (`0/0` in the drawdown ratio, not caught by the `>0` clamp) → now honest `-1.0`. Added 5 chaos tests (`tests/quantlab/test_a0_2_engine_chaos.py`) and stabilized 2 deadline-flaky engine PBTs (`ISSUE-A0-PBT-ASOF-001` pattern). Evidence: A0 `16 passed`, mypy clean; regression detectors green (integration 4, governance 25, showcase 28, family-eval 7, optimizer 5). `no_alpha_claim`.
  - **CR-B-CHAOS-001 (Implemented, repo-side):** [b chaos CR](./b-data-platform/change-requests/cr-b-chaos-001-snapshot-resilience.md) — `scripts/daily_snapshot.py::_write` is now atomic (temp + `os.replace`) so a mid-write crash can't leave a truncated record that the append-only/immutable SKIP rule preserves forever as corrupt vintage; added 5 chaos tests (`tests/test_daily_snapshot_chaos.py`: multi-source timeout cascade, write atomicity, malformed-200 payloads). Evidence: B `49 passed`. Stooq policy unchanged.
  - **H-4 live-rerun API (Requirements draft, SDD Phase 1):** [h-live-rerun-api](./h-live-rerun-api/requirements.md) — `continue active spec` next Epic H slice over the H-3 static-replay UI. Requirements fold the gap findings into ACs + seed FMEA: real-backend smoke path (false-green guard, FMEA-H4-01), async lifecycle states, component-test coverage, a public PIT-provider accessor to kill `provider._prices` (REQ-H4-007), and a charter guard (REQ-H4-008) against the deferred Lane 2 actionable-signal surface. `[Impacts: h-interactive-research-ui, b-data-platform]`. Design/impl pending.
  - **Issue log:** 6 holding rows added (`ISSUE-DDD-ANEMIC-001`, `ISSUE-DRY-CONFIG-001`, `ISSUE-REF-GODFILE-002`, `ISSUE-DDD-PROVIDER-PRIVATE-001`, `ISSUE-VRT-PY-REPORTS-001`, `ISSUE-CHAOS-MUT-REGISTER-001`) — all unowned refactor-only debt with routing + promotion thresholds.
  - **Verification caveat:** the full 435-test suite exceeds ~9 min in this environment (Hypothesis-heavy) and was not run end-to-end this session; validated via targeted chaos + consumer/regression suites. A clean full-suite + mutation refresh should be captured before promoting `dev`→`main`.
- **Current branch lane:** `spec/h-interactive-research-ui` (Epic H slice H-3, implemented repo-side/static replay; review passed locally; **deploy parity proven** — the H-3 dev lane landed on `main` (`49a4510`), Pages built it, public probe `proven`/`matched` for `dataHash c33da57d…` on 2026-06-18).
- **H interactive research UI (slice H-3):** [h-interactive-research-ui](./h-interactive-research-ui/) is **Implemented · Review PASSED (repo-side/local static replay)**. It adds an interactive parameter workflow over the existing H experiment/report/registry artifacts, with deterministic lineage, `no_alpha_claim`, OOS-net-only leaderboard boundaries, approximate-data warnings, local-first UI/API/smoke/VRT test obligations, and fail-closed public/static proof behavior. It does **not** claim JAX/TF real backends, GPU/native models, live public execution, or production Tier3 readiness. Public Pages parity is now **`proven`** — after the H-3 dev lane landed on `main` (`49a4510`) Pages served the refreshed artifact and the live probe matched deployed==expected `dataHash c33da57d…` (`proven`/`matched`, observed `2026-06-18T07:47Z`); the dashboard self-claim stays `not_proven` by design.
- **H deep-learning real training (slice H-2):** [h-deep-learning-real-training](./h-deep-learning-real-training/) is **Implemented · Review PASSED** (repo-side + torch UAT). Makes the resolved `pytorch` backend actually train the reference MLP via a new `quantlab/models/dl/torch_trainer.py` (lazy torch, float64, seed-init parity ≤`1e-3`, deterministic) reached through an additive `NumpyMLPForecaster` backend dispatch; runs in an optional default-skipped torch lane (`pytest.importorskip`) mirroring the LSTM lane, with honest `reference` fallback when torch is absent. Torch stays out of the default lock; framework-isolation (import-linter "DL backend boundary") KEPT. **Count resync done** (this closeout): canonical no-skip pytest **424→430** (torch-enabled capture), import-linter **87/237→88/242** (new module), Python mutation stays **118** published (the 2 new H-2 mutations are torch-UAT-only, both KILLED). The dashboard payload was regenerated and the visual baseline re-pinned; public hosting was later re-proven on the final refreshed `dataHash e5794260…`. `no_alpha_claim`. Deferred (later H slices): JAX/TF real backends, GPU/larger/native models, live UI, production Tier3.
- **H deep-learning research lab (slice H-1):** [h-deep-learning-research-lab](./h-deep-learning-research-lab/) is **Implemented · Review PASSED**. New Epic H research-capability lane: a framework-free deterministic `NumpyMLPForecaster` reference model (PIT-safe, per-epoch learning trace) + `DeepForecastAllocationStrategy` A0 adapter; a `FrameworkAdapterRegistry` (`quantlab/models/dl/backends.py`) that resolves real PyTorch/JAX/TensorFlow backends when installed and **degrades honestly to `reference`** when absent (never raises for a known label), keeping the framework-isolation rule intact (new import-linter "DL backend boundary" contract; both contracts KEPT); a deterministic checksummed statistical performance report (`quantlab/research/model_performance_report.py` — OOS-net authority, distribution/rolling-Sharpe/drawdown/learning-curve; fail-closed on degenerate input); a self-contained SVG/HTML visualization (`model_report_viz.py`, no CDN/script); and a parameterized experiment CLI (`scripts/run_dl_experiment.py`) that runs the model + dumb baseline through the A0 engine, builds the report + viz, registers `ExperimentRegistry` MLOps lineage (deterministic idempotent `experiment_id`), and fails closed with `status=insufficient_data` (exit 2). Everything `no_alpha_claim`; deep history is the CR-B21 approximate backfill in research mode (strict PIT unaffected). The 22 H tests + 3 H mutations moved the single-source counts on top of the CR-RDO-005 baseline: pytest **402→424** (torch-enabled no-skip), Python mutation **115/115→118/118**, mypy **→69 files**, import-linter **→88 files / 242 deps** (2 contracts), implemented epics **9→10**. The dashboard payload/static visual refresh was later completed and public hosting is now `proven` on `dataHash e5794260…` (dashboard self-claim stays `not_proven` by design). Deferred (later H slices): real torch/jax/tf GPU training, a live interactive parameter UI, and production Tier3 MLOps.
- **Released CR-RDO-005 lane:** `spec/real-data-oos-backtest` (CR-RDO-005 overlay lane) — multi-cycle, multi-asset OOS-net evaluation across the D model families, overlaying the completed `real-data-oos-backtest` + `d-model-family-evaluation` baselines.
- **CR-RDO-005 multi-cycle multi-asset family OOS-net (repo-side closure):** [CR-RDO-005](./real-data-oos-backtest/change-requests/cr-rdo-005-multi-cycle-multi-asset-oos.md) — new `quantlab/research/multi_cycle_oos.py` + `scripts/run_multi_cycle_oos_backtest.py` run a BuyAndHold baseline plus the regime / return-risk / robust D families through **one shared** real co-temporal universe/window, reusing every single-window honesty guard (co-temporal sufficiency, sampling-frequency oversampling, flat-OOS degeneracy) and emitting a checksummed leaderboard. The real run over the CR-B21 deep backfill (`approximate_availability=True`) is **computed**: co-temporal `{2330.TW, SPY, ^GSPC, ^IXIC, ^TWII}` over **2000-01-04→2026-06-12 (317mo)** covering dot-com/GFC/COVID/2022 (regime now **full-feature** — `T10Y2Y` yield curve backfilled, see CR-B21 residual closure below); OOS-net Sharpe regime **0.6694** > BuyAndHold **0.6569** > forecast **0.3540** > robust **0.3208**, `no_alpha_claim`, mechanism+comparability evidence (NOT true PIT, NOT a verdict). A clean refactor extracted the shared checksum helper `quantlab/research/oos_artifact.py` (committed single-window artifact checksum unchanged). Repo-side evidence: 28 new tests (unit/PBT/integration/e2e/smoke/chaos, all green in the default env), 4 new mutations all killed in-session, mypy clean, import-linter KEPT. Artifact: `real-data-oos-backtest/reports/multi-cycle-family-oos-artifact.json`. **Mutation evidence refreshed (done):** the full Python mutation suite now regenerates end-to-end at **123/123** — this required repairing 5 pre-existing **stale hosting-mutation anchors** (`public-hosting-{manifest,probe}-{status,hash}-overclaim` + the review-probe copy were pinned to `configured_not_observed`/`mismatched` from before the CR-B21 deploy re-pinned the committed manifest/probe to `proven`/`matched`, which had silently blocked any end-to-end mutation re-run) plus the 4 CR-RDO-005 mutations; `gate-python-mutation.json` and the 6 mutation-sync surfaces are synced to 123/123. **Pytest evidence refreshed (done):** the canonical no-skip pytest gate was captured in a torch-enabled venv (the PyTorch LSTM lane runs, no skip) → `gate-pytest.txt` **430 passed**; the review-gate count surfaces (TESTS.md, both generation guides, `docs/review/index.html`, manuals, registry) are synced to 424. **Still pending (deploy-gated only):** the committed dashboard payload (`docs/**/showcase.json`, `dataHash 0f170441…`) + its manifest/probe + browser-visual VRT re-pin still lag at 374/111 — regenerating them flips committed hosting `proven`→`configured_not_observed` until the branch deploys to Pages, so that re-pin is intrinsically deploy-coupled (mirrors CR-RDO-004) and is left for the deploy refresh.
- **CR-DME-001 model-family-evaluation claim-silent baseline (Implemented · Review PASSED):** [CR-DME-001](./d-model-family-evaluation/change-requests/cr-dme-001-claim-silent-baseline.md) — CR overlay on the completed `d-model-family-evaluation` baseline resolving `ISSUE-EVAL-001`: `quantlab/models/evaluation.py::_claim_boundary` now accepts a **claim-silent dumb baseline** (e.g. `BuyAndHold`, whose metadata omits `claim_boundary`) — the row that must always stay visible in an OOS-net leaderboard — and rejects only an **explicit** non-`no_alpha_claim` overclaim. `test_d_6` ranks a claim-silent `BuyAndHold` baseline; mutation `d-model-evaluation-alpha-gate` re-pointed to the new guard and killed (suite stays 123/123); test-function count unchanged so pytest stays 402; mypy clean. Pure backend logic — no credentials/.env, no E2E/podman, no dashboard impact.
- **CR-B21 historical backfill (1990+, approximate):** [CR-B21](./b-data-platform/change-requests/cr-b21-historical-backfill.md) — `data/vintage/raw/` previously held only a few 2026 daily captures, so research backtests couldn't span business cycles. `scripts/backfill_history.py` fetches deep history (Yahoo `period1=1990-01-01`, full FRED series, NOAA ONI) into `data/vintage/raw/backfill-1990-01-01/` with every record marked **`is_approximate=true`** + `backfill=true` (NOT true PIT — `available_date`=capture date; FRED macro is latest-*revised*). Per-source degradation + retry + idempotent skip (vintage immutability). Initial run captured **18/24 sources**; a later idempotent re-run completed the residual to **24/24** (manifest `ok=6 skip=18 fail=0`) — all 11 deep Yahoo histories (^GSPC **9179 rows, 1990-01-02→2026-06-12**, plus ^IXIC/SPY/AGG/TLT/GLD/DBC/BTC-USD/2330.TW/^TWII/TWD=X), 6 FRED (FEDFUNDS/CPIAUCSL/GDPC1/UNRATE/SP500/PCOPPUSDM), NOAA ONI; the 6 FRED rate/FX series (DGS10/DGS2/T10Y2Y/NASDAQCOM/DCOILWTICO/DEXTAUS) initially hit `fail:ReadTimeout` under transient FRED throttling, then **completed on a later idempotent re-run** (manifest `fail=0`); they are now available as macro, so the CR-RDO-005 regime family runs full-feature. **REQ-B21-002:** `build_provider_from_vintage(..., approximate_availability=True)` now yields a real multi-cycle comparison — deep {^GSPC, ^IXIC} over **1990→2026 (437 months; dot-com/GFC/COVID/2022)** is `computed`, BuyAndHold OOS-net Sharpe **0.7007** vs SmaTiming **0.2264**, `no_alpha_claim`. Honesty boundary: **strict mode excludes the backfill** (the degenerate-flat-OOS CLI guard is pinned to strict true-PIT and still fails closed). Adding the CR-B21 tests + `b-backfill-approximate-marking` mutation moved the dashboard evidence list (pytest **367→374**, Python mutation **110/110→118/118**) → new `dataHash 0f170441…`, static visual baseline re-pinned (htmlHash `0f17…`-render, historical browser screenshot `16eff6f5…`, 0-pixel diff, CR-FBP-001-legal). After CR-B21 landed on `main` (`693f780`), GitHub Pages redeployed and the live probe matched (`status=proven`, deployed==expected `0f170441…`, HTTP 200, observed `2026-06-15T07:07Z`); the committed probe/review-copy/manifest `hostingEvidence` are re-pinned to `proven`. The dashboard self-claim stays `not_proven` by design (a static artifact can't self-claim deployment).
- **CR-RDO-004 sampling-frequency oversampling guard:** [CR-RDO-004](./real-data-oos-backtest/change-requests/cr-rdo-004-sampling-frequency-guard.md) — `no_alpha_claim` mechanism-honesty fix: the real-data OOS comparison ranks on OOS-net **Sharpe** sampled at the rebalance cadence, but `assess_data_sufficiency` selected the co-temporal universe by calendar overlap only, never checking that each asset *updates* at least as often as the rebalance cadence. A monthly-native asset (`PCOPPUSDM`) under a finer rebalance forward-fills stale prices into **fabricated flat-return periods**, understating volatility and inflating Sharpe. Now the research module estimates each asset's native sampling frequency (median `event_date` spacing → `classify_cadence`), records a `sampling_frequency` provenance block, and `build_real_data_oos_report` **fails closed** (`SamplingFrequencyError`) when `coarsest_native_days > rebalance_days * 1.5`; the CLI maps it to `status=insufficient_data`, `reason=oversampled_vs_native_frequency`, exit 2. The default single-index SP500 run is unaffected (homogeneous) and stays `computed`. Adding the CR-RDO-004 tests/mutation moved the dashboard evidence list (pytest **355→367**, Python mutation **109/109→110/110**) → new `dataHash c73d7c88…`, and both visual baselines were re-pinned (static htmlHash `a47139fb…`, historical browser screenshot `16eff6f5…`, 0-pixel diff, CR-FBP-001-legal). The committed probe/review-copy/manifest `hostingEvidence` honestly revert to `configured_not_observed` / `hashStatus=mismatched` (deployed `68dfae0f…` ≠ expected `c73d7c88…`) until CR-RDO-004 lands on `main` and GitHub Pages redeploys; the dashboard self-claim stays `not_proven` by design.
- **CR-FPS-011 hosting-evidence freshness determinism + refresh automation:** [CR-FPS-011](./f-public-static-showcase/change-requests/cr-fps-011-hosting-freshness-determinism.md) — fixed a latent wall-clock suite-rot time-bomb: the Python committed-evidence consumer (`scenario.py::_validate_public_hosting_probe`) raised on `observedAt + 24h < datetime.now()`, so the committed proof + test fixtures broke the build ~24h after any re-prove with no code change. Now freshness is classified against an injected `asof` and a stale observation **downgrades to `configured_not_observed`** (consistent with the CR-FPS-008 frontend contract) instead of crashing; integrity violations still hard-raise; the dashboard self-claim stays `not_proven`. Added `scripts/refresh_public_hosting_proof.py` (re-probe → rewrite committed probe/copy/manifest, `proven` only when matched+fresh, fail-closed) to replace manual re-proving. Adding the CR-FPS-011 tests/mutations moved the dashboard evidence list (pytest **355**, Python mutation **109/109**) → new `dataHash 68dfae0f…`, and both visual baselines were re-pinned (static htmlHash `c349229f…`, historical browser screenshot `16eff6f5…`, 0-pixel diff, CR-FBP-001-legal). After CR-FPS-011 landed on `main`, GitHub Pages redeployed and the new `scripts/refresh_public_hosting_proof.py` / live probe matched (`status=proven`, deployed==expected `68dfae0f…`, HTTP 200) — the committed probe/review-copy/manifest `hostingEvidence` are re-pinned to `proven`. The dashboard self-claim stays `not_proven` by design.
- **CR-RDO-003 market-index real-data OOS (landed on `main`):** [CR-RDO-003](./real-data-oos-backtest/change-requests/cr-rdo-003-market-index-availability.md) — critical finding: single-capture FRED vintage (`available_date` = capture date) is **PIT-invisible to historical as-ofs**, so the prior `{PCOPPUSDM, SP500}` "computed" comparison was **degenerate** (flat OOS). Fix: use the **market index (SP500)** with explicit `approximate_event_date` availability (NOT true PIT, marked) + a **degeneracy guard** (flat OOS → never `computed`). The real run now trades the SP500 index, **SmaTimingStrategy 0.808 vs BuyAndHold 0.877** OOS-net (buy-and-hold wins net-of-cost over the 2016–2026 bull run) — a real, honest comparison, `no_alpha_claim`.
- **CR-FPS-010 public hosting (re-proven on main at the CR-RDO-003 hash):** [CR-FPS-010](./f-public-static-showcase/change-requests/cr-fps-010-public-hosting-proven.md) — `main` is live-`proven` again. CR-RDO-003 changed the dashboard `realData` → new `dataHash a99453c3…`; after that landed on `main` GitHub Pages redeployed and the live probe matched (`status=proven`, `hashStatus=matched`, deployed==expected `a99453c3…`, HTTP 200, observed `2026-06-14T02:00Z`). The committed `docs/public-hosting-probe.json` (+ `docs/review/assets/` copy) and `docs/deployment-manifest.json` `hostingEvidence` are re-pinned to `proven`. The dashboard payload claim stays `not_proven` by design (a static artifact can't self-claim deployment). Project remains `local_demo_only` / `no_alpha_claim`.
- **CR-RDO-002 dashboard real-data surface (#2c):** [CR-RDO-002](./real-data-oos-backtest/change-requests/cr-rdo-002-dashboard-surface.md) — the showcase payload carries an optional `realData` block from the committed artifact ({PCOPPUSDM, SP500}, ranked OOS-net, visible baseline) and the dashboard renders a `real-data` panel; both visual baselines re-pinned (historical browser screenshot `16eff6f5…`, 0-pixel diff, guard-legal via CR-FBP-001); `publicHosting` stays `not_proven`. Boundary: mechanism evidence under `no_alpha_claim`, not a strategy verdict.
- **CR-FBP-001 visual baseline guard:** [CR-FBP-001](./f-browser-pixel-baseline/change-requests/cr-fbp-001-repinned-baseline-hash-guard.md) — the stale-baseline-hash check now fires only when `baselineHash != currentHash`; a 0-pixel deterministic re-pin is accepted without weakening stale-hash detection or the pixel-diff threshold. Enables the real-data-oos dashboard wire-up (#2c).
- **Real-data OOS-net backtest slice:** [real-data-oos-backtest](./real-data-oos-backtest/) is **Implemented · Review PASSED** for repo-side mechanism + honest comparison. `quantlab/research/real_data_oos.py` + `scripts/run_real_data_oos_backtest.py` run a candidate strategy and a dumb baseline through the PIT vintage loader on >=2 real price assets, emit a checksumed OOS-net comparison ranked OOS-net only with the baseline visible, and fail closed to `status=insufficient_data` (exit 2) with `no_alpha_claim` when <2 assets / thin history. Real accumulated vintage data is *sufficient*, so the CLI runs a computed comparison. [CR-RDO-001](./real-data-oos-backtest/change-requests/cr-rdo-001-cotemporal-universe.md) makes sufficiency overlap-aware and selects a co-temporal universe (the real run trades `{PCOPPUSDM, SP500}` over their shared 2016–2026 window, not a degenerate 1992-vs-2026 mix); OOS-net values remain mechanism evidence under `no_alpha_claim`, not a strategy verdict. Residual follow-up: sampling-frequency harmonization. Evidence artifact: `.agents/specs/real-data-oos-backtest/reports/real-data-oos-artifact.json`.
- **Dependency security:** Dependabot alert #7 fixed (`fixed_at=2026-06-12T01:19:57Z`) for `uv.lock` / `torch`; the default root environment still excludes the optional PyTorch lane.
- **Stakeholder docs refreshed (2026-06-13):** bilingual user manual `docs/manual/{en,zh-tw}/index.{md,html}` and executive review `docs/review/index.html`, plus generation guides (`docs/MANUAL_GENERATION_GUIDE.md`, `docs/REVIEW_GENERATION_GUIDE.md`), shared evidence/warning contracts (`docs/{FEATURES,EVIDENCE_METADATA_CONTRACT,DEMO_RISK_WARNING_TAXONOMY}.md`), and traceability bridge `.agents/specs/RTM.md`. Live CLI demos captured under `docs/manual/assets/`; gate transcripts under `docs/review/assets/`; the chromium-headless `browser-visual.png`, `browser-visual-diff.json`, `public-hosting-probe.json` (HTTP 200 plus deployed manifest hash/contract metadata), and autonomous schedule proof run `27392471359` are tracked as current evidence. The dashboard payload is generated from a canonical local `LocalResultStore` / `ExperimentRegistry` scenario with source metadata; `docs/deployment-manifest.json` and `docs/public-hosting-probe.json` are now `proven` for the refreshed `dataHash`.
- **Latest evidence:** `uv run pytest -q` → **435 passed, 2 skipped** (default root env after the E/B/F test-quality refactor slices; H-2 torch-enabled UAT remains **430 passed** with the optional torch lane running); B daily snapshot and F showcase detector checks are **0 findings / 0 high**; F Python showcase tests **28 passed**; focused B/F/governance suite **97 passed**; `uv run mypy quantlab/ scripts/build_showcase_payload.py scripts/run_tsmc_hedge_slice.py scripts/run_vintage_slice.py scripts/scheduled_run_observer.py scripts/tier3_readiness_gate.py scripts/source_quorum_proof.py scripts/stooq_contract_proof.py scripts/run_real_data_oos_backtest.py --ignore-missing-imports` → clean over 69 files; `uv run lint-imports` → KEPT over 88 files / 242 dependencies; Stooq proof/source-health focused coverage → 11 passed, 90% combined line coverage; E registry + production evidence focused coverage → 37 passed, 99% combined line coverage; Python mutation spot checks **123/123 configured/killed** including `browser-visual-doc-sync-gate-regression`; default `uv sync` removes `torch`/CUDA packages from the root env; F Next.js tests 52 passed, line coverage 84.12%, `npm audit --json` 0 vulnerabilities, 29/29 frontend mutations killed, `npm run e2e:interactive` browser fail-closed VRT passed, static export/browser visual pixel diff (`0 / 1,296,000` at threshold `0.001`)/build/smoke passed, and public probe is now **proven** with HTTP 200 / manifest 200 / hash `matched` for deployed==expected `dataHash c33da57d…` (Pages serves the refreshed artifact after the `main` deploy).
- **Test-quality detector state:** B daily snapshot tests and F showcase tests are detector-clean (**0 findings / 0 high** each) after the E/B/F cleanup slices. `ISSUE-TEST-QUALITY-LARGE-FILES-001` is closed locally; reopen only if detector findings recur in actively edited large test files or failure triage again depends on scanning long assertion clusters.
- **Promotion proof boundary:** do not append every squash PR to this rolling memo. Authoritative promotion state lives in GitHub PR state and spec-local reports; `NEXT_STEPS.md` only records current resume state, durable evidence boundaries, and links to owning specs/reports so future squash promotions do not create another stale memo loop.
- **2026-06-18 correction:** the current default-root Python gate is `uv run pytest -q` → **435 passed, 2 skipped**. The **430 passed** count is retained only as H-2 torch-enabled UAT / optional-lane evidence, not as the default-root count. The prior static dashboard payload was re-proven at `dataHash e5794260…`; H-3 regenerated the branch-local payload to `dataHash c33da57d…`, which is now **`proven`** on Pages after the `main` deploy (`49a4510`).
- **2026-06-18 public probe (proven):** after the H-3 lane landed on `main` (`49a4510`) GitHub Pages built the commit and `npm run probe:public-demo` returns **`proven`** with HTTP 200 / manifest 200 / hash `matched` (deployed==expected `c33da57d…`, observed `2026-06-18T07:47Z`); the committed proof was refreshed via `scripts/refresh_public_hosting_proof.py --live`. `ISSUE-RDO5-001` remains resolved for the prior lane; the H-3 deploy-coupled public re-proof is now **closed** (`ISSUE-H3-PUBLIC-PROOF-001`).
- **Latest external Actions proof:** `daily-snapshot.yml` failed before fix as run `27386918387` because `github.run_started_at` expanded empty. Fixed branch run `27387041974` on `spec/b-live-scheduled-snapshot-proof` succeeded via `workflow_dispatch`. Autonomous cron run `27392471359` on `main` later succeeded with `event=schedule` and uploaded `snapshot-schedule-proof`; proof JSON records `dry=22`, `exit_code=0`, `evidence_tier=smoke`, `retention=append_only`, `trigger=schedule`, and artifact digest `a2699a2ae7b24175364ce448633f789a03cd5557da2470b091ad7652409ffe61`.
- **ISSUE-B3-001 handled in advance:**
  - Promoted/folded into [CR-B7 source health](./b-data-platform/change-requests/cr-b7-source-health.md) for invalid FRED gold proxy defaults.
  - Repo-side fix: default FRED price proxy list now uses reachable `PCOPPUSDM` commodity proxy instead of invalid London gold IDs.
  - Promoted/folded into [CR-B8 Yahoo chart fallback](./b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md) for TSMC/TWSE fallback capture and PIT loading.
  - Promoted/folded into [CR-B9 Stooq opt-in](./b-data-platform/change-requests/cr-b9-stooq-opt-in.md); Stooq is no longer a default daily snapshot source.
  - Residual: Stooq itself remains external/source-contract blocked; TSMC/TWSE fallback is live-smoke proven through Yahoo chart from this environment.
- **Epic D first slice:** [d-first-regime-model](./d-first-regime-model/) is **Implemented(first slice) · Review PASSED**.
  - Added `RegimeSignal`, `RegimeFeatureBuilder`, `FirstRegimeClassifier`, and `RegimeAllocationStrategy`.
  - Targeted D evidence: 6 tests for PIT-safe signal and OOS-net baseline integration.
  - Conservative writeup: synthetic data proves pipeline correctness only; no alpha claim.
- **Epic C C-3:** time/regime rebalance selector implemented additively in `quantlab/portfolio/rebalance.py`; A0 engine scheduling now consumes it through CR-A0.
- **A0/C regime scheduling:** [CR-A0](./a0-backtest-foundation/change-requests/cr-a0-regime-rebalance-scheduling.md) lets the vectorized engine execute C-3 regime-selected dates through serializable `rebalance_policy` labels.
- **A0 low-frequency event replay:** [CR-A0 event replay](./a0-backtest-foundation/change-requests/cr-a0-event-driven-replay.md) replaces the prior `event_driven` `NotImplementedError` stub with explicit `event_dates` replay under existing PIT/cost/metric semantics; high-frequency/order-book simulation remains future scope.
- **Mutation automation:** [mutation automation report](./a0-backtest-foundation/reports/mutation-automation-report.md) originated `scripts/run_mutation_spot_checks.py`; current suite is governed by the latest evidence row above (**123/123 configured/killed**, including the H deep-learning research lab OOS-net-ranking / no-alpha-claim / honest-backend-fallback guards, CR-RDO-005 multi-cycle multi-asset OOS-net guards, CR-B21 historical-backfill honesty-boundary guard, CR-RDO-004 real-data OOS sampling-frequency guard, CR-A0 event replay, and cross-spec governance mutations).
- **D-3 real-source-format benchmark:** [real-data regime benchmark report](./d-first-regime-model/reports/real-data-regime-benchmark-report.md) adds vintage-loader-based OOS-net baseline comparison with explicit `no_alpha_claim`.
- **F showcase first slice:** [f-showcase-read-api-dashboard](./f-showcase-read-api-dashboard/) is **Implemented(repo-side read API/dashboard payload) · Review PASSED**.
  - Added `ShowcaseReadAPI`, dashboard summary builder, and deterministic HTML smoke renderer.
  - Live-demo readiness is **CONDITIONAL / hybrid** until a real Next.js runtime and browser evidence exist.
- **D return/risk second model slice:** [d-return-risk-forecast-model](./d-return-risk-forecast-model/) is **Implemented · Review PASSED**.
  - Added deterministic PIT-safe `ReturnRiskForecaster`, `ForecastAllocationStrategy`, and OOS-net benchmark helper.
  - Conservative writeup: no alpha claim; Tier3 MLOps remains deferred.
- **F real Next.js dashboard:** [f-nextjs-showcase-dashboard](./f-nextjs-showcase-dashboard/) is **Implemented · Review PASSED** for local runtime proof.
  - Added contained `frontend/` Next.js app, `/api/showcase`, component tests, mutation check, build, and local HTTP smoke.
  - Public hosting remains deferred; visual regression is now repo-side browser-proven through CR-FPS-009.
- **D robust optimizer third model family:** [d-robust-portfolio-optimization-model](./d-robust-portfolio-optimization-model/) is **Implemented · Review PASSED**.
  - Added downside-penalized robust optimizer strategy and OOS-net benchmark helper.
- **E Tier3 readiness / E-lite:** [e-mlops-tier3-readiness.md](./e-mlops-tier3-readiness.md) moved E to planning-ready; [e-mlops-tier3-lite](./e-mlops-tier3-lite/) is **Implemented · Review PASSED** for registry-only experiment lineage.
- **E Tier3 readiness gate:** [e-tier3-readiness-gate](./e-tier3-readiness-gate/) is **Implemented · Review PASSED** for repo-side false-green prevention; `tier3_ready` is impossible unless serving, retraining, and automated drift monitoring evidence are all `status=proven`.
- **E Tier3 serving evidence:** [e-tier3-serving-evidence](./e-tier3-serving-evidence/) is **Implemented · Review PASSED** for repo-side local serving smoke evidence; it is explicitly `evidence_tier=local_smoke` and cannot satisfy production Tier3 readiness.
- **E Tier3 retraining evidence:** [e-tier3-retraining-evidence](./e-tier3-retraining-evidence/) is **Implemented · Review PASSED** for repo-side local retraining smoke evidence; it is explicitly `evidence_tier=local_smoke` and cannot satisfy production Tier3 readiness.
- **E Tier3 production evidence gate:** [e-tier3-production-evidence-gate](./e-tier3-production-evidence-gate/) is **Implemented · Review PASSED** for repo-side production-tier evidence gating and local automated drift monitoring smoke evidence; Tier3 readiness now requires `status=proven`, correct `readiness_evidence_for`, and `evidence_tier=production` for serving, retraining, and automated drift monitoring.
- **E Tier3 production probes:** [e-tier3-production-probes](./e-tier3-production-probes/) is **Implemented · Review PASSED** for governed production evidence builders/validators; production serving rejects localhost/non-HTTPS identities, production retraining rejects local/incomplete runs, and production drift monitoring rejects local/unsupported monitor payloads. This prepares external proof ingestion but does not itself execute production services.
- **E Tier3 readiness proof CLI:** [e-tier3-readiness-proof-cli](./e-tier3-readiness-proof-cli/) is **Implemented · Review PASSED** for strict file-based production proof validation; it emits a Tier3 readiness gate artifact only after manifest, serving, retraining, and drift evidence JSON all pass governed validators and the manifest artifact URI is external; anything short of that remains `not_ready`.
- **B source-health follow-up:** [CR-B10](./b-data-platform/change-requests/cr-b10-source-health-registry.md) is **Implemented(repo-side)**; source status summaries are explicit and do not re-enable blocked Stooq defaults.
- **F demo hardening:** [f-demo-hardening](./f-demo-hardening/) is **Implemented · Review PASSED**; dashboard exposes `local_demo_only`, `publicHosting=not_proven`, `visualRegression=proven`, and dependency audit posture after CR-FPS-009 wired the browser visual proof into the payload.
- **B snapshot reliability / CR-B11:** [CR-B11](./b-data-platform/change-requests/cr-b11-snapshot-run-report.md) is **Implemented(repo-side) · Review PASSED**; `scripts/daily_snapshot.py --report-json` emits machine-readable counts, per-source failures, and source-health posture while keeping Stooq blocked/default-disabled.
- **B scoped live write smoke / CR-B12:** [CR-B12](./b-data-platform/change-requests/cr-b12-live-write-smoke.md) is **Implemented(repo-side + local live smoke)**.
  - `scripts/daily_snapshot.py` now supports scoped source selection and `--out-root`, so live-write smoke can prove append-only writes without broad source fragility or repo data pollution.
  - Live smoke on 2026-06-12 wrote `fred_FEDFUNDS.json` with `ok=1 fail=0`; a second run produced `skip=1`, proving immutable same-day behavior. `snapshot_ops_gate.py` accepted both non-dry-run reports as `clean`.
  - Boundary: proves scoped live append-only write mechanics, not broad default source availability and not Stooq readiness.
- **B source quorum gate / CR-B18:** [CR-B18](./b-data-platform/change-requests/cr-b18-source-quorum-gate.md) is **Implemented(repo-side)**.
  - `scripts/snapshot_ops_gate.py --require-source-quorum` now requires a non-dry-run, zero-failure report with live `ok`/`skip` coverage across FRED macro, FRED price-proxy, Yahoo equity, Yahoo market, and NOAA groups.
  - Unit, CLI smoke, PBT, chaos, and mutation coverage reject scoped CR-B12 smoke, dry-run-only reports, replayed dry rows, failed critical sources, and missing source groups as broad source readiness.
  - Boundary: this defines the gate for future broad-source proof; it does not itself prove broad default source availability or re-enable Stooq.
- **B source quorum proof / CR-B19:** [CR-B19](./b-data-platform/change-requests/cr-b19-source-quorum-live-proof.md) is **Implemented(repo-side + live proof)**.
  - `scripts/source_quorum_proof.py` runs the FRED/Yahoo/NOAA quorum scope, writes a proof artifact, and exits nonzero unless the snapshot command succeeds, CR-B18 passes, and every quorum `ok`/`skip` row has a matching snapshot file.
  - Live proof on 2026-06-12 wrote/confirmed `fred_FEDFUNDS`, `fred_SP500`, `fred_PCOPPUSDM`, `yahoo_2330.TW`, `yahoo_idx_TWII`, and `noaa_oni`; final proof is `status=proven`, `evidence_tier=live_source_quorum`, `fail=0`, `skip=6`.
  - Boundary: Stooq remains blocked/default-disabled and outside this proof until a working Stooq source contract and live close rows are proven.
- **B Stooq contract proof / CR-B20:** [CR-B20](./b-data-platform/change-requests/cr-b20-stooq-contract-proof-wrapper.md) is **Implemented(repo-side + live fail-closed proof)**.
  - `scripts/stooq_contract_proof.py` runs an explicit Stooq-only opt-in probe and exits 0 only when append-only snapshot files contain positive finite close rows.
  - Live proof on 2026-06-12 for `spy.us` returned HTTP 404 and emitted `status=not_proven`, `rows=[]`, and `decision=requires_live_close_rows`.
  - Boundary: even a successful future Stooq proof only reaches `eligible_for_opt_in_review`; default enablement remains separate maintainer policy.
- **E/F registry dashboard bridge:** [e-f-registry-dashboard-bridge](./e-f-registry-dashboard-bridge/) is **Implemented · Review PASSED**; F read API and Next.js dashboard display E-lite registry entries as `research_only` / `registry_only` / `no_alpha_claim`.
- **F public demo readiness:** [f-public-demo-readiness](./f-public-demo-readiness/) is **Implemented · Review PASSED** for local production-demo readiness; PostCSS advisory is remediated through npm override, audit is clean, and `npm run smoke` validates `/` plus `/api/showcase`. Public hosting remains `not_proven`; visual regression is `proven` by the repo-side browser visual diff gate.
- **G alt-data first slice:** [g-alt-data-first-slice](./g-alt-data-first-slice/) is **Implemented · Review PASSED**; optional local CSV loader requires source authority/pin, is default-disabled, and enforces `available_date <= asof`.
- **F public static showcase:** [f-public-static-showcase](./f-public-static-showcase/) is **Implemented · Review PASSED**; GitHub Pages static `docs/` artifact path is configured, `frontend/out` export is reproducible, visual contract baseline is pinned, [CR-FPS-001](./f-public-static-showcase/change-requests/cr-fps-001-hosting-manifest-proof-sync.md) keeps the committed `docs/deployment-manifest.json` aligned with tracked HTTP 200 public-hosting proof, [CR-FPS-002](./f-public-static-showcase/change-requests/cr-fps-002-hosting-content-hash-proof.md) requires deployed `dataHash` parity, [CR-FPS-003](./f-public-static-showcase/change-requests/cr-fps-003-hosting-manifest-contract-proof.md) requires manifest-contract parity, [CR-FPS-004](./f-public-static-showcase/change-requests/cr-fps-004-dashboard-evidence-freshness.md) keeps static dashboard gate evidence current, [CR-FPS-005](./f-public-static-showcase/change-requests/cr-fps-005-deployment-catchup-proof.md) records deployed content-hash/manifest-contract parity after `main` deployment caught up, [CR-FPS-006](./f-public-static-showcase/change-requests/cr-fps-006-canonical-dashboard-source.md) replaces the inline dashboard fixture with a generated canonical local result-store artifact, [CR-FPS-007](./f-public-static-showcase/change-requests/cr-fps-007-public-probe-parity-status.md) makes the standalone public probe fail closed while the deployed `dataHash` is stale, [CR-FPS-008](./f-public-static-showcase/change-requests/cr-fps-008-public-probe-freshness-gate.md) requires fresh standalone probe observations before public hosting can be `proven`, and [CR-FPS-009](./f-public-static-showcase/change-requests/cr-fps-009-dashboard-visual-readiness-wireup.md) wires browser visual proof into the dashboard payload without upgrading public hosting.
- **B snapshot ops gate:** [b-snapshot-ops-gate](./b-snapshot-ops-gate/) is **Implemented · Review PASSED**; machine-readable reports are validated for count consistency, Stooq blocked/default-disabled posture, and explicit partial-failure handling.
- **G alt-data second slice:** [g-alt-data-second-slice](./g-alt-data-second-slice/) is **Implemented · Review PASSED**; second optional default-disabled source contract and bundle PIT loader are in place.
- **E registry durability bridge:** [e-registry-durability-bridge](./e-registry-durability-bridge/) is **Implemented · Review PASSED**; registry snapshots have checksums and `LocalResultStore` bridge consumes real OOS-net run records.
- **D model-family evaluation:** [d-model-family-evaluation](./d-model-family-evaluation/) is **Implemented · Review PASSED**; D family comparison ranks OOS-net only, keeps baseline visible, and rejects alpha-claim records.
- **Next gaps 1-6 Tier3/Public/Ops:** [next-gaps-1-6-tier3-public](./next-gaps-1-6-tier3-public/) is **Implemented · Review PASSED** in the working tree.
- F public hosting proof: GitHub Pages is configured for `main` `/docs`; `https://austinyuch.github.io/finance_algorithms/` returned HTTP 200 and deployed `deployment-manifest.json` returned HTTP 200. The H-3 committed proof is now **`proven`** — deployed==expected `dataHash c33da57d…` after the `main` deploy (`49a4510`), observed `2026-06-18T07:47Z`.
  - F browser visual proof: `docs/browser-visual.json` records Chromium screenshot hash `6d4e84420e766e93d7f703ac09d1f0391eaeb9a367d501fbb051b8a61e5c1b0f`.
  - E Tier3 first slice: non-serving run manifest and drift skeleton only; no serving, retraining, or automated drift monitoring claim.
  - B scheduled ops: append-only schedule report helper and latest pointer are unit-tested.
  - D evaluation: family evaluator can consume real `LocalResultStore` records.
  - B Stooq: source-contract decision helper keeps blocked/default-disabled posture until live close rows are proven.
- **Ops/visual/drift artifacts lane:** [ops-visual-drift-artifacts](./ops-visual-drift-artifacts/) is **Implemented · Review PASSED**.
  - B scheduled ops proof: workflow config and proof builder record workflow/trigger/command/exit status, smoke/live tier, and append-only retention.
  - F visual diff: browser visual evidence now emits thresholded diff status while preserving screenshot hash proof.
  - E drift assessment: metric-delta drift report is assessed-not-automated, with `serving_status=not_serving` and `retraining_status=not_configured`.
  - B source-contract reopen: Stooq remains default-disabled; live close rows only allow `eligible_for_opt_in_review`.
  - D artifact expansion: model-family evaluation can be wrapped in checksumed JSON with row-count and OOS-net authority validation.
- **F browser pixel baseline lane:** [f-browser-pixel-baseline](./f-browser-pixel-baseline/) is **Implemented · Review PASSED**.
  - Replaced hash-derived visual mismatch with a committed PNG baseline and real pixel mismatch ratio.
  - Latest browser visual diff: `0 / 1,296,000` mismatched pixels, `mismatchRatio=0.0008310185185185185`, threshold `0.001`.
  - Dashboard remains `local_demo_only`; CR-FPS-006 reduces fixture-heavy payload risk by using a generated local result-store scenario, while this lane closes the visual-diff false-green residual only.
- **B live scheduled snapshot proof lane:** [b-live-scheduled-snapshot-proof](./b-live-scheduled-snapshot-proof/) is **Implemented · Review PASSED**.
  - Fixed the GitHub Actions workflow timestamp bug exposed by failed run `27386918387`.
  - Real Actions runs `27387041974` (`workflow_dispatch`) and `27392471359` (`schedule`) succeeded with artifact `snapshot-schedule-proof`.
  - Boundary: proves autonomous cron dry-run smoke execution. CR-B12 separately proves scoped local live append-only write mechanics.
- **B scheduled run observer lane:** [b-scheduled-run-observer](./b-scheduled-run-observer/) is **Implemented · Review PASSED** and merged to `dev`/`main`.
  - Added `scripts/scheduled_run_observer.py` to classify GitHub Actions run-list evidence without overclaiming manual dispatches.
  - Live observation artifact: `reports/scheduled-run-observation-2026-06-12-cron.json` records `status=proven`, `schedule_run_count=1`, and latest schedule success `27392471359`.
  - Boundary: observer proves cron trigger classification only; CR-B12 separately proves scoped local live-write mechanics, while broad source availability remains governed separately.
- **A Torch default dependency isolation lane:** [a-torch-default-dependency-isolation](./a-torch-default-dependency-isolation/) is **Implemented · Review PASSED** and merged to `dev`/`main`.
  - Removed unpatched `torch<=2.12.0` from default root `pyproject.toml` / `uv.lock` after Dependabot alert #7 reported no patched version.
  - `scripts/run_tsmc_hedge_slice.py` now degrades honestly without Torch and emits an optional PyTorch-lane notice.
  - Boundary: root default dependency reachability is closed; GitHub Dependabot alert #7 is fixed as of `2026-06-12T01:19:57Z`.

## Recommended Next Action

1. Keep `daily-snapshot.yml` observer evidence current after future cron runs; do not regress autonomous schedule proof from run `27392471359` back to pending without fresher contradictory Actions evidence.
2. **Stooq is de-scoped — do not pursue (not worth it).** Re-diagnosed live 2026-06-15: the `/q/l/` CSV quote endpoint returns HTTP 404 for *every* symbol (incl. `aapl.us`, so the endpoint was removed, not a symbol error) and the `/q/d/l/` history endpoint now sits behind a JavaScript anti-bot challenge. Stooq has **no API key to apply for**; restoring it would need a fragile headless-browser JS-challenge bypass (likely ToS-violating) with no payoff — FRED/Yahoo/NOAA already cover the proven source quorum. Stooq stays blocked/default-disabled permanently; `scripts/stooq_contract_proof.py` is retained only as a fail-closed guard, not a backlog item.
3. Use `scripts/tier3_readiness_gate.py` for any external E production proof handoff; local smoke artifacts, invalid JSON, hand-written production-looking maps, plain proof IDs, and bare orchestrator/monitor labels must remain nonzero failures.
4. Keep stakeholder docs copied from `review.md` verdicts and avoid deriving readiness from test counts.
5. **CR-B21 backfill residual — DONE.** The 6 FRED rate/FX series (DGS10/DGS2/T10Y2Y/NASDAQCOM/DCOILWTICO/DEXTAUS) were completed by an idempotent re-run of `scripts/backfill_history.py --since 1990-01-01` (manifest `ok=6 skip=18 fail=0`). `T10Y2Y` is now available, so the **CR-RDO-005 regime family runs full-feature** (no longer price-trend-only); the committed multi-cycle artifact was regenerated (regime OOS-net Sharpe `0.6811→0.6694`, still edges BuyAndHold, ranking order unchanged). Research-mode only (`is_approximate=true`); strict-PIT runs unaffected. **Deploy-coupled stakeholder docs** (`docs/manual/*`, `docs/review/index.html`, `docs/FEATURES.md`, generation guides) still narrate the prior `18/24` / regime-degraded state and refresh on the deploy alongside the dashboard `dataHash` re-pin (see ISSUE-RDO5-001).
6. **CR-RDO-005 evidence-refresh — DONE.** Mutation, canonical default-root pytest evidence, dashboard payload/static export, browser-visual proof, and public Pages parity were refreshed at `dataHash e5794260…`. H-3 superseded the branch-local payload with `dataHash c33da57d…`, which is now **`proven`** on Pages (see item 7).
7. **H interactive research UI — DONE (incl. public proof).** [h-interactive-research-ui](./h-interactive-research-ui/) is implemented and review-passed for the local/static-replay dashboard slice: static-replay payload, local UI controls, contract tests, smoke assertions, visual proof, browser fail-closed VRT (`npm run e2e:interactive`), and H-3 frontend mutations are in place. **Public Pages parity is now `proven`** — after the H-3 lane landed on `main` (`49a4510`) Pages served `dataHash c33da57d…` and the live probe matched (`proven`/`matched`, observed `2026-06-18T07:47Z`); committed proof refreshed via `scripts/refresh_public_hosting_proof.py --live`. The dashboard self-claim stays `not_proven` by design.

8. **Chaos CR closeout (2026-06-19 — repo-side DONE; the published-count deploy-coupled remainder LANDED 2026-06-21 at pytest 478 / mutation 123 / `dataHash 6c18e572…`, see top entry).**
   - (a) **DONE.** Clean full-suite transcript captured: `uv run pytest -q` → **446 passed, 2 skipped** (`.agents/specs/a0-backtest-foundation/reports/fullsuite-2026-06-19-chaos-closeout.txt`). The transcript **caught a real regression** that the prior session's incomplete suite missed: `test_multi_cycle_oos.py::test_chaos_nan_closes_do_not_crash` failed on `dev` because CR-A0-CHAOS-001's `_close` fix removed an accidental fail-closed signal (a NaN-poisoned asset would silently enter a multi-cycle comparison at a fabricated weight). **Fixed** at the shared sufficiency layer — `real_data_oos.py::_asset_spans` now builds spans from usable (finite, positive) closes only, so a NaN asset drops out of the co-temporal universe and fails sufficiency closed (FMEA-A0-CHAOS-04). Multi-cycle + all `real_data_oos*` suites **79 passed**; full suite green.
   - (b) **DONE (registration + proof).** 4 chaos guards registered in `scripts/run_mutation_spot_checks.py` and proven KILLED: `a0-chaos-close-finite-positive-guard`, `a0-chaos-total-loss-drawdown-fillna`, `a0-chaos-asset-span-usable-close-filter`, `b-chaos-snapshot-atomic-write` (`ISSUE-CHAOS-MUT-REGISTER-001`).
   - (c) **DONE.** `quantlab/TESTS.md` rows added for `test_a0_2_engine_chaos` (6 pass) and `test_daily_snapshot_chaos` (5 pass); governance guards 25 + mutation-runner test green.
   - (d) **DONE.** `## Review verdict` (Implemented · Review PASSED) authored in both CR docs.
   - **Deploy-coupled remainder — DONE 2026-06-21 (actual landed counts: pytest `435`→`478` and Python mutation `118`→`123`; the earlier `446`/`122` projection was pre-capture):** published the new counts — across the governance/dashboard surfaces (`quantlab/TESTS.md` count line, `docs/review/assets/gate-python-mutation.json`, `docs/showcase.json` + manual/frontend/review payload copies, `CORRECTNESS_CHECKLIST.md`, `NEXT_STEPS.md`/`RTM.md`/`SPECS.md`/`ISSUE_LOG.md`/`FEATURES.md`, gate transcripts) and re-pin the visual baseline. These live in the live `showcase.json` payload, so they intrinsically flip hosting `proven`→`configured_not_observed` until Pages redeploys (mirrors CR-RDO-005).
9. **H-4 live-rerun API — Phase 2 design DONE; next is Phase 3 tasks.** [h-live-rerun-api](./h-live-rerun-api/) now has [design.md](./h-live-rerun-api/design.md) + [contract/live-rerun.schema.json](./h-live-rerun-api/contract/live-rerun.schema.json) (SDD Phase 2): a thin Python ASGI backend (`quantlab/showcase/rerun_service.py`) wrapping `run_experiment`, a Next.js `app/api/experiment/rerun` proxy route with H-3 `static_replay` fallback, a TS lifecycle reducer (`idle/computing/computed/fail_closed/error`) with a bounded `AbortController` timeout, a public PIT-provider view (`symbols`/`event_dates`/`price_panel`) that kills `provider._prices` reach-ins (REQ-H4-007 / `ISSUE-DDD-PROVIDER-PRIVATE-001`), a **real-backend dynamic-port smoke + negative stub-fails-closed test** (FMEA-H4-01, load-bearing), and full FMEA-H4-01..07. **H-4 is Implemented (repo-side) · Review PASSED** ([review.md](./h-live-rerun-api/review.md)). Delivered: public PIT-provider view (H4-1, resolves `ISSUE-DDD-PROVIDER-PRIVATE-001`); Python ASGI rerun backend wrapping `run_experiment` (H4-2); Next.js proxy route + static-replay fallback (H4-3); TS lifecycle reducer + bounded client (H4-4); `live_compute` contract widen (H4-5); 5-state `LiveRerunStatus` component (H4-6); **real-backend uvicorn dynamic-port smoke proving fresh compute + negative stub-fails-freshness (H4-7, FMEA-H4-01)**; charter guard (H4-9); determinism + Python mutations (H4-8/H4-10). **Deploy-coupled remainder — DONE 2026-06-21 (PRs #135/#136):** static byte-parity re-pin, browser VRT baselines + the new live UI controls' static-export/visual re-pin (`ISSUE-VRT-EXPORT-STALE-DEV-001` resolved), frontend mutation count, and the pytest/mutation count surfaces all landed at `dataHash 6c18e572…` (public hosting `proven`). Old continuation note follows for history: ~~Next: backend (H4-2)~~ → route/fallback (H4-3) → lifecycle reducer (H4-4) → contract (H4-5) → component tests (H4-6) → **load-bearing real-backend smoke + negative stub (H4-7)** → determinism/parity (H4-8) → charter guard (H4-9) → mutation (H4-10) → VRT (H4-11) → closeout (H4-12). Next: TDD implementation. Lane 2 (actionable current-asof signal) stays charter-gated/deferred (REQ-H4-008).

## Scheduled Ops

- Daily vintage snapshot routine is expected to continue writing append-only files under `data/vintage/raw/<date>/`.
- Current live proof says routine/source health is partial but broader than the earlier scoped smoke: Yahoo fallback is proven for `2330.TW` and `^TWII`; CR-B12 proves scoped live append-only write/skip mechanics for `fred:FEDFUNDS`; CR-B19 proves the FRED/Yahoo/NOAA broad source quorum on 2026-06-12. CR-B20 proves Stooq restoration attempts fail closed without positive finite live close rows; Stooq remains opt-in/blocked.

## Resume Hints

- For D closure truth, read [d-first-regime-model/review.md](./d-first-regime-model/review.md), [writeup.md](./d-first-regime-model/writeup.md), and [reports/implementation-report.md](./d-first-regime-model/reports/implementation-report.md).
- For D2 closure truth, read [d-return-risk-forecast-model/review.md](./d-return-risk-forecast-model/review.md) and [reports/implementation-report.md](./d-return-risk-forecast-model/reports/implementation-report.md).
- For F closure truth, read [f-showcase-read-api-dashboard/review.md](./f-showcase-read-api-dashboard/review.md) and [reports/implementation-report.md](./f-showcase-read-api-dashboard/reports/implementation-report.md).
- For B-3 source status, read [ISSUE_LOG.md](./ISSUE_LOG.md), [b-data-platform/change-requests/cr-b7-source-health.md](./b-data-platform/change-requests/cr-b7-source-health.md), [b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md](./b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md), and [b-data-platform/change-requests/cr-b9-stooq-opt-in.md](./b-data-platform/change-requests/cr-b9-stooq-opt-in.md).
- For C-3 closure truth, read [c-portfolio-core/reports/c3-rebalance-report.md](./c-portfolio-core/reports/c3-rebalance-report.md).
- For A0 regime scheduling, read [a0-backtest-foundation/change-requests/cr-a0-regime-rebalance-scheduling.md](./a0-backtest-foundation/change-requests/cr-a0-regime-rebalance-scheduling.md).
- For mutation automation, read [a0-backtest-foundation/reports/mutation-automation-report.md](./a0-backtest-foundation/reports/mutation-automation-report.md).
- For D-3 benchmark truth, read [d-first-regime-model/reports/real-data-regime-benchmark-report.md](./d-first-regime-model/reports/real-data-regime-benchmark-report.md).
- For B CR-B11, read [b-data-platform/change-requests/cr-b11-snapshot-run-report.md](./b-data-platform/change-requests/cr-b11-snapshot-run-report.md).
- For E/F bridge, read [e-f-registry-dashboard-bridge/review.md](./e-f-registry-dashboard-bridge/review.md).
- For F public-demo local readiness, read [f-public-demo-readiness/review.md](./f-public-demo-readiness/review.md).
- For G alt-data, read [g-alt-data-first-slice/review.md](./g-alt-data-first-slice/review.md).
- For test truth, read [quantlab/TESTS.md](../../quantlab/TESTS.md) then [.agents/specs/TESTS.md](./TESTS.md).

## Key Locked Decisions

- 個人自用純紙上;成功=方法論誠實度 + 實驗能力,非 alpha;雙目的(作品集 + lab)→ 兩速結構。
- 每模型 DoD:A0 產出「可與笨 baseline 並排比較的 OOS 報告」。
- 三框架(PyTorch/TF/JAX)harness 無感;Tier1+2 進 A0,Tier3(完整 MLOps)延後。
