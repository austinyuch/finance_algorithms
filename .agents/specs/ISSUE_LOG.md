# ISSUE_LOG.md — Improvement Holding Surface

> Holds unresolved improvement items that are not yet safely closed by an active spec, CR, or review verdict. This is not a second `SPECS.md` and not a task dashboard.

| Issue ID | State | Cluster Key | Observation | Candidate Owner | Evidence | Routing Recommendation | Promotion Threshold |
|---|---|---|---|---|---|---|---|
| DOC-RECON-2026-06-21 | Closed (no-op) | governance-doc-reconciliation | 2026-06-21 doc reconciliation after the **H-4 live-rerun-API deploy completed** (PR #135 payload+counts, PR #136 re-prove). The deploy-coupled residuals that DOC-RECON-2026-06-20 + `tasks.md` H4-12 left bundled for the next Pages deploy are now **landed**: regenerated `docs/showcase.json` → `dataHash 6c18e572…`, public hosting **`proven` / `matched`** (live-probed 2026-06-21T04:18Z, observed in `docs/public-hosting-probe.json`), and the published snapshot advanced to **pytest 478 / frontend 70 / Python mutation 123/123** across every guard-coupled + stakeholder surface. This pass then flipped the remaining **deploy-pending → deployed** wording in `README.md`, `RTM.md` (H-4 verdict), `SPECS.md` (H-3 hash `c33da57d…`→`6c18e572…` + H-4 row), `h-live-rerun-api/tasks.md` (H4-11/H4-12 closed), `NEXT_STEPS.md` (new 2026-06-21 Current-State entry; superseded the abandoned-PR#131 / `446`-`122` projection bullets), `AGENTS.md` (deploy-coupling worked-example), and the manual/review footers + guides. Reconciles `ISSUE-VRT-EXPORT-STALE-DEV-001` (VRT export-stale, resolved by the deploy). The earlier **abandoned PR #131** standalone bump is retained as history — coupling to a real deploy is what converged (runbook captured in agent memory). | — (docs only; deploy-coupling owned by `f-public-static-showcase` + `governance-evidence-refresh`) | `docs/public-hosting-probe.json` (`status=proven`, `hashStatus=matched`, deployed==expected `6c18e572…`, observed `2026-06-21T04:18Z`); live Pages serves `6c18e572`; governance guards `25 passed`; full suite `478 passed, 2 skipped`. | No promotion — pure stakeholder-doc + governance-memo resync of already-owned, now-deployed work (H-4). Recorded for audit trail. | Reopen only if a later doc pass finds a count/hosting/H-4 gap with no owning spec/CR, or if a payload refresh changes `dataHash` without a matching Pages re-proof. |
| DOC-RECON-2026-06-20 | Closed (no-op) | governance-doc-reconciliation | 2026-06-20 doc reconciliation after the **H-4 live-rerun-API** merge (PR #128), the **mutation-gate refresh** merge (PR #129, resolves `ISSUE-MUT-GATE-ROT-001` + `ISSUE-MUT-RUNNER-FALSEGREEN-001`), and the **abandoned count-payload bump** (PR #131 closed, non-converging). Documented the **deploy-coupling / count-refresh policy** in `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md` + `AGENTS.md`; recorded the 2026-06-20 promotions/deferrals in `NEXT_STEPS.md`; **fixed two stale present-tense hosting claims** (the NEXT_STEPS deploy-coupled-deltas bullet + `quantlab/TESTS.md` line 6) that read `configured_not_observed`/`e5794260` when the committed live probe is `proven`/`matched`/`c33da57d…` (2026-06-18 deploy); bumped manual/review HTML footers to 2026-06-20 with an H-4 deploy-pending note. **No count cascade** — pytest **435** + mutation **118** stay the guard-coupled published snapshot; governance SoT mutation **123** is a documented delta. **Follow-up (same day, PR #132 already merged):** propagated the H-4 slice into the non-count-coupled traceability/catalog surfaces that `tasks.md` H4-12 had bundled into the deploy — added an H-4 row to `RTM.md`, feature #12 + de-staled the H-1/H-3 "live rerun deferred" boundary notes in `docs/FEATURES.md`, and an Epic H clause in `README.md`, each marked **Implemented repo-side · Review PASSED, deploy-pending**; `tasks.md` H4-12 updated to reflect these landed repo-side (leaving only count/`dataHash`/visual/probe + manual-review body content in the deploy bundle). | — (docs only; deploy-coupling owned by `f-public-static-showcase` + `governance-evidence-refresh`) | `docs/public-hosting-probe.json` (`status=proven`, `hashStatus=matched`, deployed==expected `c33da57d…`, observed `2026-06-18T07:47Z`); `.agents/specs/{RTM,SPECS}.md` (proven `c33da57d`); governance guards `25 passed` this pass; README clean (no count mentions). | No promotion — pure stakeholder-doc + governance-memo resync of already-owned merged work (H-4, mutation refresh) plus a documented abandonment (count-payload). Recorded for audit trail. | Reopen only if a later doc pass finds a count/hosting/H-4 gap with no owning spec/CR, or if the deploy-coupled count bump is re-attempted standalone (the non-converging PR #131 path). |
| ISSUE-DDD-ANEMIC-001 | Open (holding) | anemic-domain-model | 2026-06-19 spec-master gap pass: core results cross module boundaries as bare `dict[str, Any]` / `list[dict]` with string keys instead of typed value objects, so invariants (e.g. portfolio weights sum to 1.0, OOS-net metric shape) are not enforced at call sites and governance guards rely on string-key matching. No active owner; refactor-only, no behavior bug observed. | `c-portfolio-core` / a future domain-modeling spec | `quantlab/engine/vectorized.py:40-42` (backtest result dict), `:24` + `quantlab/runner.py:13-15` (config `Mapping[str,Any]`), portfolio weights `dict[str,float]`, `quantlab/engine/metrics.py` (`list[dict]` metrics) | Keep in holding. Address opportunistically when one of these modules is next edited (fold into that lane); promote to a `domain-model` spec only if a real invariant-violation bug is traced, not for aesthetics. | Promote only if a defect is caused by an unenforced invariant, or 3+ future lanes are blocked by the dict-passing. |
| ISSUE-DRY-CONFIG-001 | Open (holding) | config-duplication-drift | 2026-06-19 gap pass: `_COST_CONFIG` (×4 scripts), `_WALK_FORWARD` (×2), and cadence/min-history magic numbers are copy-pasted across `scripts/run_*.py` and `quantlab/research`/`engine`, with no shared constants module → silent governance drift if one copy changes. Refactor-only. | `governance-evidence-refresh` / owning research+script lanes | `scripts/run_real_data_oos_backtest.py:38-40`, `run_multi_cycle_oos_backtest.py:43-45`, `run_dl_experiment.py:95`, `run_vintage_slice.py:45`; `quantlab/research/real_data_oos.py:38-39`, `quantlab/engine/vectorized.py:20` | Hold. Extract a `quantlab.config` (or `quantlab.research.constants`) SSOT when the next lane touches one of these scripts; reuse there. | Promote to a small refactor CR if a real cost/walk-forward drift between two copies is observed. |
| ISSUE-REF-GODFILE-002 | Open (holding) | god-file-residual | 2026-06-19 gap pass: residual large mixed-responsibility modules after the 2026-06-18 splits (ISSUE-REF-MUTATION-HARNESS-001 / ISSUE-REF-E-MLOPS-REGISTRY-001). Effective but hard to audit; no behavior bug. | `test-quality-reviewer` advisory + owning specs | `scripts/run_mutation_spot_checks.py` (1130 lines), `quantlab/showcase/scenario.py` (580: I/O+validation+build), `quantlab/mlops/experiment_registry.py` (591) | Hold; keep cleanup opportunistic + tied to touched files (same policy as ISSUE-TEST-QUALITY-LARGE-FILES-001). Use `code-refactoring-advisor` as advisory when one is next edited. | Promote to a refactor slice only when a god file is actively edited and a duplicated-responsibility/anchor cluster recurs. |
| ISSUE-DDD-PROVIDER-PRIVATE-001 | Resolved (H4-1) | encapsulation-boundary | 2026-06-19 gap pass found research + scripts reaching into `provider._prices` (private) for co-temporal analysis. **Resolved by H-4 task H4-1**: `InMemoryPITDataProvider` now exposes a public read view (`symbols()`, `event_span()`, `price_panel(symbols, *, usable_only)`); every `_prices` reach-in in `real_data_oos`, `run_dl_experiment`, and `run_vintage_slice` is migrated to it, and a grep guard (`test_h4_provider_view.py::test_no_module_reaches_into_provider_private_prices`) keeps it closed. The usable-close filter is consolidated into `price_panel`. | folded into H-4 (h-live-rerun-api) | `quantlab/data/provider.py` (public view), `quantlab/research/real_data_oos.py`, `scripts/run_dl_experiment.py`, `scripts/run_vintage_slice.py`, `tests/quantlab/test_h4_provider_view.py` | Closed — public accessor shipped + grep-guarded. | — (resolved) |
| ISSUE-VRT-PY-REPORTS-001 | Open (holding, low) | python-report-vrt | 2026-06-19 gap pass: generated Python SVG/HTML research reports have only structural HTML smoke coverage; no pixel/visual-regression. Frontend dashboard pixel-diff already covers the public surface, so value is low for a paper-only lab. | `ops-visual-drift-artifacts` / owning report specs | `quantlab/research/model_report_viz.py`, `quantlab/showcase/html.py`; frontend VRT at `frontend/scripts/browser-visual-smoke.mjs` covers the dashboard only | Hold as Low. Add a deterministic SVG-hash check only if a report-rendering regression is ever observed. | Promote only if a Python-generated report visually regresses unnoticed. |
| ISSUE-MUT-GATE-ROT-001 | Resolved (2026-06-20 refresh) | mutation-gate-staleness | **RESOLVED 2026-06-20**: the full Python mutation suite is runnable end-to-end again and **re-validated to 123/123** (torch-excluded). The rot had hidden since the E-MLOPS refactor: 9 E-production mutations had their source moved to `production_evidence.py` AND tests moved to `test_e_production_evidence.py`; 5 more e-tier3 mutations + `yahoo-latest-close` had silently-stale **test selectors** (the false-green bug hid them — ISSUE-MUT-RUNNER-FALSEGREEN-001); 11 governance/hosting drift anchors were re-authored to current values; and one genuine test-quality gap was found+fixed (`f-showcase-visual-diff-contract-gate` survived because the test never set a wrong `artifactKind` — added that case). The 2 torch-UAT mutations are excluded via the new `--exclude` flag. | `governance-evidence-refresh` / mutation automation | `scripts/run_mutation_spot_checks.py`, `quantlab/mlops/production_evidence.py`, `tests/quantlab/test_e_production_evidence.py`, `tests/quantlab/test_f_1_showcase_api.py` | Closed — anchors repaired (source+test+verified real kill), full suite re-validated 123/123, published count bumped 118→123. | — (resolved) |
| ISSUE-MUT-RUNNER-FALSEGREEN-001 | Resolved (2026-06-20) | mutation-runner-integrity | **RESOLVED 2026-06-20**: `run_mutation` now treats pytest exit 5 ("no tests collected", e.g. a stale/renamed selector) as a hard **ValueError**, not a KILL — fail-closed instead of false-green. Regression test added (`test_run_mutation_fails_closed_when_test_command_collects_zero`). This bug is what had hidden the gate rot; fixing it surfaced 6 additional stale selectors during the refresh (ISSUE-MUT-GATE-ROT-001). | mutation automation / `governance-evidence-refresh` | `scripts/run_mutation_spot_checks.py` (`run_mutation` exit-5 guard), `tests/test_mutation_spot_checks.py` | Closed — fail-closed guard + regression test shipped. | — (resolved) |
| ISSUE-DDD-QUANTLAB-IMPORTS-SCRIPTS-001 | Open (holding, low) | layering-inversion | 2026-06-20 (H4-2): `quantlab/showcase/rerun_service.py` imports `from scripts.run_dl_experiment import run_experiment` — a `quantlab` package depending on a top-level `scripts/` module (layering inversion). It works (lazy import; tests already import `scripts.run_dl_experiment`) and import-linter does not flag it (its contracts cover framework isolation only), but the deterministic experiment orchestration arguably belongs in `quantlab/research/` so library code never depends on `scripts/`. | h-live-rerun-api / a future research-packaging refactor | `quantlab/showcase/rerun_service.py` (`run_rerun`, `build_default_provider`); `scripts/run_dl_experiment.py::run_experiment` | Hold (low). When `run_dl_experiment` is next substantially edited, lift `run_experiment` into `quantlab/research/dl_experiment.py` and have the CLI + service import from there; add an import-linter contract forbidding `quantlab -> scripts`. | Promote to a refactor CR if another `quantlab` module needs `run_experiment`, or if a `quantlab→scripts` import ever breaks packaging/install. |
| ISSUE-VRT-EXPORT-STALE-DEV-001 | Open (deploy-coupled) | static-export-visual-lag | 2026-06-20 (H-4 frontend pass): on **pristine `dev` HEAD** the frontend test `public-demo.test.tsx > keeps committed static export artifacts aligned with the canonical dashboard payload` **fails** — the committed `docs/visual-snapshot.json` htmlHash no longer matches the freshly-rendered `Dashboard` HTML. Proven pre-existing (fails with all H-4 changes stashed). This is the documented deploy-coupled dashboard-payload/visual re-pin lag manifesting as a red test on the integration branch (a false-green/CI-hygiene gap: `dev`'s frontend suite is not fully green between deploys). | f-public-static-showcase / f-browser-pixel-baseline | `frontend/tests/public-demo.test.tsx:57`, `docs/{showcase,visual-snapshot,deployment-manifest}.json` | Do **not** regenerate mid-branch — re-pinning the visual snapshot changes `dataHash` and flips hosting `proven`→`configured_not_observed`. Bundle the export/visual re-pin with the user-gated `dev`→`main` deploy (same step as the pytest/mutation count bump). | Close when the deploy re-pin lands on `main` and Pages re-proves; promote to a fix CR if it persists green-on-main but red-on-dev across >1 deploy cycle. |
| ISSUE-CHAOS-MUT-REGISTER-001 | Registered · count-bump deploy-coupled | chaos-guard-mutation-coverage | 2026-06-19: **4 chaos guards now registered in `scripts/run_mutation_spot_checks.py` and proven KILLED** — `a0-chaos-close-finite-positive-guard`, `a0-chaos-total-loss-drawdown-fillna`, `a0-chaos-asset-span-usable-close-filter` (CR-A0-CHAOS-001, incl. the downstream `_asset_spans` sufficiency fix), and `b-chaos-snapshot-atomic-write` (CR-B-CHAOS-001). Runner suite is now 122 specs; the **published** total stays `118` until the deploy-coupled bump. | `governance-evidence-refresh` / mutation automation | `quantlab/engine/{vectorized,metrics}.py`, `quantlab/research/real_data_oos.py`, `scripts/daily_snapshot.py`, `scripts/run_mutation_spot_checks.py`; CRs `cr-a0-chaos-001-*`, `cr-b-chaos-001-*` | Registration complete. The only remainder is publishing `118`→`122` across the count surfaces, which is deploy-coupled (lives in the live `showcase.json` payload) — bundle with the `dev`→`main` dataHash re-pin (see NEXT_STEPS item 8). | Close when the deploy-coupled count bump lands on `main` and Pages re-proves. |
| DOC-RECON-2026-06-18-H2 | Closed (no-op) | governance-evidence-resync | 2026-06-18 Epic H slice H-2 (`h-deep-learning-real-training`) closeout: real PyTorch training added (`quantlab/models/dl/torch_trainer.py` + additive `dl_forecaster` dispatch), proven in a torch-enabled UAT (full suite **430 passed**, lane 4 passed, 2 mutations KILLED, parity ≤`1e-3`, determinism). Count resync applied as one deploy-coupled bundle: canonical no-skip pytest **424→430** (torch capture), import-linter **87/237→88/242** (new module), mutation stays **118** published (the 2 H-2 mutations are torch-UAT-only). Regenerated dashboard payload (`dataHash 4237842a…`), static visual baseline (htmlHash `74e23199…`), browser-visual re-pin (`16eff6f5…`, 0-pixel), and recorded the then-deploy-gated probe/manifest state; public hosting was later re-proven on final `dataHash e5794260…`. All 25 governance guards green. `no_alpha_claim`. Owned by H-2 (SPECS/RTM/NEXT_STEPS rows added). | — (owned by H-2) | `quantlab/models/dl/torch_trainer.py`, `tests/quantlab/test_h2_*`, `scripts/run_mutation_spot_checks.py`, `.agents/specs/{SPECS,NEXT_STEPS,RTM,TESTS}.md`, `quantlab/{TESTS,CORRECTNESS_CHECKLIST}.md`, `docs/{FEATURES,review,manual,index.html,showcase.json,deployment-manifest.json}`, `frontend/{lib,visual-baselines}` | No promotion: owned spec H-2; JAX/TF real backends, GPU/native models, live UI, production Tier3 deferred (recorded in SPECS/NEXT_STEPS). Recorded for audit trail. | Reopen only if a later H pass finds a gap with no owning spec/CR. |
| DOC-RECON-2026-06-18-H3-STAKEHOLDER-DOCS | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-18 stakeholder-doc reconciliation for the **owned, review-PASSED Epic H slice H-3** (`h-interactive-research-ui`). H-3 was already woven into `SPECS.md` / `RTM.md` / `NEXT_STEPS.md` and `ISSUE-H-INTERACTIVE-UI-001` promoted; this pass propagated it into the remaining stakeholder surfaces: added an Interactive Research UI feature row (#11) to `docs/FEATURES.md` and corrected the stale "live interactive UI deferred" boundary on the Epic H row; noted H-3 in `AGENTS.md` (Epic H architecture rule) and the `README.md` Epic H paragraph; added a bilingual **Flow 8 — Interactive research UI** to the manual (`docs/manual/{en,zh-tw}/index.{md,html}`) with nav entries, a resolved-gap bullet, and footer dates; added an H-3 core-feature card + resolved-gap entry (EN+ZH) and footer date to `docs/review/index.html`; and documented the H-3 evidence path in `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md`. Readiness copied from `h-interactive-research-ui/review.md` (PASSED, repo-side/local static-replay; public Pages parity deploy-gated at expected `dataHash c33da57d…`). Manual HTML headless-chromium render-validated (en/zh, no residual). `no_alpha_claim`; `static_replay` only — no live backend rerun, JAX/TF real training, GPU/native models, or production Tier3. No new **unowned** items surfaced. | — (docs only) | `docs/FEATURES.md`, `AGENTS.md`, `README.md`, `docs/manual/{en,zh-tw}/index.{md,html}`, `docs/review/index.html`, `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md`; readiness source `.agents/specs/h-interactive-research-ui/review.md` | No promotion: pure stakeholder-doc resync of an already-owned, review-PASSED slice (H-3). Recorded for audit trail only. | Reopen only if a later doc pass finds an H-3 / interactive-UI gap with no owning spec/CR. |
| ISSUE-H3-PUBLIC-PROOF-001 | Resolved (public parity proven) | governance-evidence-resync | The H-3 `review.md` carried a deploy-coupled residual: public Pages parity stayed `configured_not_observed` / `mismatched` because Pages still served the prior `dataHash e5794260…` while the committed H-3 expected hash was `c33da57d…`. **Resolved 2026-06-18:** after the H-3 dev lane landed on `main` (`49a4510`), GitHub Pages built the commit (`pages/builds/latest` `status=built`, deployed `c33da57d…`) and the live probe re-ran **`proven`** / **`matched`** (HTTP 200 / manifest 200, deployed==expected `c33da57d…`, observed `2026-06-18T07:47Z`). Committed proof refreshed via `scripts/refresh_public_hosting_proof.py --live` (`docs/{public-hosting-probe,deployment-manifest}.json` + `docs/review/assets/public-hosting-probe.json`), and the now-stale `configured_not_observed` prose was flipped to proven across review.md / NEXT_STEPS / SPECS / RTM / FEATURES / manual / review (dashboard self-claim left `not_proven` by static-artifact contract; generated showcase payloads untouched so the committed `dataHash` keeps matching Pages). Same deploy-coupled pattern as the resolved `ISSUE-RDO5-001`. | `h-interactive-research-ui` (active lane) + `governance-evidence-refresh` | `npm run probe:public-demo` → `proven`; `scripts/refresh_public_hosting_proof.py --live`; `docs/public-hosting-probe.json` (`status=proven`, `hashStatus=matched`); governance guards 25/25 + F showcase + H3 tests green. | No new spec/CR — owned by the active H-3 lane plus governance evidence refresh. | Reopen only if a later dashboard payload refresh changes `dataHash` without a matching Pages re-proof, or if public-hosting proof becomes stale/mismatched. |
| ISSUE-VIS-CI-BASELINE-001 | Deferred (by design — local-first CI) | visual-baseline-history | The manual/review visual-gap inventories list "no CI-managed visual baseline history beyond the repo baseline" (owner `f-browser-pixel-baseline`, Implemented · Review PASSED). Resolving it would require a hosted-CI mechanism that maintains visual-baseline history across runs, which **conflicts with the repo's local-first CI policy** (`AGENTS.md` "Local-First CI Policy": minimize hosted GitHub Actions; the visual diff is already repo-baseline pixel-backed and browser-proven). For a personal, paper-only research lab the repo-baseline pixel diff (`1077 / 1,296,000` at threshold `0.001`, `npm run visual:browser` `proven`) is sufficient. Classified during the 2026-06-18 spec-master visual-gap pass as **deferred-by-design**, not promoted. | `f-browser-pixel-baseline` (owns the residual) | `f-browser-pixel-baseline/review.md`; `docs/browser-visual-diff.json`; `AGENTS.md` Local-First CI Policy | No promotion: a hosted-CI baseline-history build is explicitly out of scope under local-first CI; the repo-baseline pixel diff covers the need. Remains a documented Low/by-design residual in the manual + review. | Reopen only if the project adopts hosted CI for visual baselines or the repo-baseline pixel diff proves insufficient for an actively-edited UI surface. |
| ISSUE-FPS-FRESH-001 | Resolved (test stabilized) | hosting-freshness-boundary-flake | `tests/quantlab/test_f_1_showcase_api.py::test_pbt_hosting_freshness_window` was a flaky Hypothesis PBT: it asserted `proven` iff `age_hours < 24.0`, but at `age_hours ≈ 24.0` the assertion could disagree with the classifier after `isoformat()` precision effects. Surfaced 2026-06-17 during H-2 work but was **entirely independent of H-2**. Fixed 2026-06-18 by generating integer second offsets, disabling deadline sensitivity, and asserting the same inclusive 24h stale boundary as `_classify_hosting_freshness` (`age_seconds < 24*60*60` is fresh; exactly 24h is stale). | `f-public-static-showcase` (CR-FPS-008/011 freshness baseline) | `tests/quantlab/test_f_1_showcase_api.py::test_hosting_freshness_window_boundary_is_inclusive`; `tests/quantlab/test_f_1_showcase_api.py::test_pbt_hosting_freshness_window`; targeted pytest 2026-06-18: F PBT + F boundary + A0 PBT `3 passed`; focused F+A0 module run `23 passed`; full suite `uv run pytest -q` → `422 passed, 2 skipped`. | No new spec / CR needed: test-only stabilization aligned the PBT oracle with the existing completed F freshness contract and did not change production behavior. | Reopen only if the F freshness PBT recurs after the integer-second oracle fix. |
| ISSUE-A0-PBT-ASOF-001 | Resolved (test stabilized) | pit-provider-pbt-runtime-flake | `tests/quantlab/test_a0_1_pit_dataprovider.py::test_pbt2_no_row_after_asof` intermittently failed in full-suite runs but passed in isolation. The invariant itself remains correct (`get(asof)` must never return a row whose `available_date > asof`); the likely flake mode is Hypothesis runtime/deadline sensitivity around repeated provider construction/import under full-suite load, not an A0 PIT logic breach. Fixed 2026-06-18 by adding explicit Hypothesis settings (`max_examples=50`, `deadline=None`) while leaving the invariant assertion unchanged. | `a0-backtest-foundation` (PIT data provider baseline) | `tests/quantlab/test_a0_1_pit_dataprovider.py::test_pbt2_no_row_after_asof`; isolated A0 module run 2026-06-18: `4 passed`; targeted F/A0 PBT run 2026-06-18: `3 passed`; focused F+A0 module run `23 passed`; full suite `uv run pytest -q` → `422 passed, 2 skipped`. | No new spec / CR needed: test-only stabilization on completed A0 baseline; production PIT gate unchanged. | Reopen only if the invariant fails with a concrete counterexample after deadline removal; promote to A0 CR only if evidence shows provider behavior, not test runtime, is wrong. |
| ISSUE-GOV-EVIDENCE-DRIFT-001 | Resolved | governance-evidence-resync | Current evidence surfaces mixed torch-enabled UAT/no-skip counts with default-root evidence. Fixed locally on 2026-06-18 by refreshing the default-root pytest transcript to `434 passed, 2 skipped`, regenerating the canonical showcase payload/static export (`dataHash e5794260…`), re-pinning the static visual hash, refreshing browser visual evidence (`16eff6f5…`, 0-pixel), syncing stakeholder payload copies, and relabeling test registries / feature surfaces so **430 passed** remains H-2 torch-enabled UAT evidence rather than default-root proof. Follow-up tightened `quantlab/CORRECTNESS_CHECKLIST.md` to the same default-root-vs-UAT split and added a guard marker for the stale checklist phrase. After publishing `04b467a` to `main`, GitHub Pages built the commit and the committed public-hosting proof was refreshed to `proven` / `matched` for `dataHash e5794260…`. | `governance-evidence-refresh` + `test-registry-manager` | `uv run pytest -q` → `434 passed, 2 skipped`; `uv run pytest -q tests/quantlab/test_governance_guards.py` → `25 passed`; `npm run visual:browser` → mismatchRatio `0`; `npm run probe:public-demo` → `proven` / HTTP 200 / manifest 200 / hash `matched`; `docs/{deployment-manifest,public-hosting-probe}.json` and review probe copy refreshed. | No new product spec. Local registry/doc/payload drift and public parity are closed. | Reopen only if a default-root vs torch-UAT count is mislabeled again, if public-hosting proof becomes stale/mismatched, or if derived docs drift from `quantlab/TESTS.md` / `docs/review/assets/gate-pytest.txt` / `quantlab/CORRECTNESS_CHECKLIST.md`. |
| ISSUE-REF-MUTATION-HARNESS-001 | Resolved (refactor-only) | refactor-mutation-harness | `scripts/run_mutation_spot_checks.py` had grown into a large mixed-responsibility harness and duplicated the public-hosting mutation proof command/anchors across seven adjacent `MutationSpec` blocks. Fixed 2026-06-18 by extracting a shared public-hosting proof command plus `_public_hosting_proof_mutation(s)` builders while preserving mutation names, order, anchors, list/apply/restore semantics, and report behavior. | `governance-evidence-refresh` / mutation automation | Added characterization coverage in `tests/test_mutation_spot_checks.py` for the public-hosting mutation family. Verification: `uv run pytest -q tests/test_mutation_spot_checks.py` → `11 passed`; selected public-hosting mutation family → `7/7` killed, `survived=0`; normal guard `test_public_hosting_manifest_carries_observed_proof` → `1 passed`. | No new product spec. Remaining harness growth, if any, should be handled as future refactor slices only when another mutation family shows duplicated anchors or responsibilities. | Reopen only if mutation list/order/report behavior drifts, or if the next mutation-family addition again requires duplicating public-hosting proof anchors. |
| ISSUE-REF-E-MLOPS-REGISTRY-001 | Resolved (boundary extraction) | refactor-e-mlops-boundaries | `quantlab/mlops/experiment_registry.py` had combined experiment registry durability, result-store bridge, production evidence validators, local smoke builders, and report helpers. Fixed 2026-06-18 by extracting production-tier evidence URI/timestamp/digest validators plus production serving/retraining/drift builders into `quantlab/mlops/production_evidence.py`, re-exporting through the existing `quantlab.mlops` surface, and preserving persisted JSON shapes plus Tier3 fail-closed semantics. | `e-mlops-tier3-lite` / E production evidence specs | Focused E/Tier3 verification: `uv run pytest -q tests/quantlab/test_e_1_experiment_registry.py tests/test_tier3_readiness_gate_cli.py` → `42 passed`; `uv run mypy quantlab/ --ignore-missing-imports` → clean over 62 source files; `uv run lint-imports` → KEPT over 89 files / 247 deps. | No new product spec. Remaining local-smoke/drift/result-store extraction can be future refactor-only slices if those areas are touched again. | Reopen only if production evidence behavior, public imports, persisted artifact shapes, or Tier3 readiness semantics drift. |
| ISSUE-TEST-QUALITY-LARGE-FILES-001 | Resolved (detector-clean for E/B/F touched slices) | test-quality-maintenance | Large test files remain effective but can be difficult to audit. Reduced the E slice on 2026-06-18 by moving production-tier evidence tests from `tests/quantlab/test_e_1_experiment_registry.py` into `tests/quantlab/test_e_production_evidence.py`, aligned with the extracted `quantlab/mlops/production_evidence.py` boundary. Reduced F conditional-test smells by making the hosting-freshness PBT setup unconditional, replacing branch-shaped expected assertions with an explicit freshness lookup, extracting browser visual-contract invalid cases into explicit parametrized partitions, moving leaderboard PBT store setup behind a helper, splitting dashboard-summary default/immutability assertions, removing the real external URL literal from the public-probe fixture while preserving the production target URL contract, splitting the dashboard HTML smoke assertion cluster into focused structure and warning/claim-boundary checks, labeling clustered artifact assertions, and removing stale-fixture wording that triggered fragile-string detection. Reduced B conditional/repeatability/assertion-smell findings by splitting the snapshot-count PBT into explicit missing-jobs, no-successful-work, and matching-count success partitions, replacing branch-shaped fake HTTP with explicit response maps, building scheduled-run URL fixture values through local helpers instead of external URL literals, aligning Yahoo PBT observations so generated timestamps and close values stay paired without branch-shaped repair logic, removing source-quorum fixture/replay loops that the detector classified as conditional test logic, replacing Stooq CSV literal fixtures with a helper, and labeling remaining clustered assertions. | `test-registry-manager` + owning specs B/F | E registry file reduced from 2224 lines / 37 tests to 1463 lines / 24 tests; new production evidence file has 768 lines / 13 tests. F detector is now 0 findings / 0 high; `uv run pytest -q tests/quantlab/test_f_1_showcase_api.py` → `28 passed`. B detector is now 0 findings / 0 high; `uv run pytest -q tests/test_daily_snapshot.py` → `44 passed`. Verification 2026-06-18: `uv run pytest -q` → `434 passed, 2 skipped`; focused combined E coverage command → `37 passed`, 99% combined line coverage. Existing B/F coverage remains intact while the detector no longer reports residual maintainability findings. | No new product spec. Keep future test-quality cleanup opportunistic and tied to touched files; preserve PBT/integration/smoke/chaos/VRT coverage intact. | Reopen only if B/F/E detector findings recur in actively edited large test files or failure triage again depends on scanning long assertion clusters. |
| ISSUE-COV-PYTHON-GLOBAL-001 | Resolved (governed policy) | coverage-governance | The repo has strong focused line-coverage evidence and mutation gates, but global Python coverage is not yet a readiness threshold. Fixed 2026-06-18 by adding an explicit policy to `quantlab/TESTS.md` and the workspace rollup: focused coverage rows are the current authority; the full-suite `--cov=quantlab --cov=scripts` command is diagnostic-only until it completes reliably and an explicit threshold is approved. | `governance-evidence-refresh` / local-first CI | Focused coverage authority remains: F 94%, E registry 99%, B source-health 97.6%, G 100%, daily snapshot 100%, frontend 90%. Diagnostic global attempt `uv run pytest --cov=quantlab --cov=scripts --cov-report=term --cov-report=json:/tmp/python-global-coverage.json -q` was interrupted after 385s, surfacing stale `test_mutation_spot_checks` row drift (`10` vs `11`) before completion; registry row corrected and guarded. Verification target: `test_quantlab_test_registry_governance_rows_match_current_test_inventory`. | No new product spec. Do not claim a global Python coverage percentage or threshold until a future coverage CR chooses a stable command, acceptable runtime, and threshold. | Reopen only if a readiness surface claims global Python line coverage without a completed governed command/threshold, or if local-first CI starts treating the diagnostic inventory command as mandatory. |
| ISSUE-H-INTERACTIVE-UI-001 | Promoted to spec (requirements draft) | h-research-ui-next-slice | The most valuable feature-level next slice after governance closeout is a live/interactive research UI over existing Epic H artifacts and real-data OOS evidence. The blocking deploy-gated evidence and registry drift were reconciled on 2026-06-18, so the holding row was promoted into a formal SDD requirements draft. | [h-interactive-research-ui](./h-interactive-research-ui/requirements.md) | H-1/H-2 review/NEXT_STEPS defer live UI, JAX/TF real backends, GPU/native models, and production Tier3. Existing H evidence is framework-isolated, optional torch, and `no_alpha_claim`. Public hosting proof is now `proven` for `dataHash e5794260…`, and the UI slice inherits F public proof/freshness and E Tier3 fail-closed boundaries. | Continue through SDD Phase 1 approval, then Phase 2 design. Requirements define interactive parameter workflows, artifact lineage, no-alpha warnings, frontend/API tests, smoke/VRT, and fail-closed stale evidence behavior. | Closed as a holding issue. Reopen only if the H interactive requirements are rejected and no replacement spec/CR owner is selected. |
| DOC-RECON-2026-06-17-CR-B21-DOC-SYNC | Closed (no-op) | governance-evidence-resync | 2026-06-17 stakeholder-doc reconciliation of the **completed CR-B21 backfill residual**. The 6 previously-throttled FRED rate/FX series (DGS10/DGS2/T10Y2Y/NASDAQCOM/DCOILWTICO/DEXTAUS) were captured by the 2026-06-16 idempotent re-run — committed manifest is `ok=6 skip=18 fail=0` (24/24 present, `T10Y2Y` available → regime family full-feature) — but the **stakeholder docs still narrated the pre-residual `18/24 / fail=6 / 6 FRED pending throttle` state**. Reconciled the narrative to `24/24 / fail=0 / regime full-feature` across 8 surfaces: the evidence transcript `docs/manual/assets/backend-historical-backfill-01-demo.txt`, the bilingual manual (`docs/manual/{en,zh-tw}/index.{md,html}`), `docs/FEATURES.md`, `docs/MANUAL_GENERATION_GUIDE.md`, and the executive review `docs/review/index.html` (EN+ZH gap lists). `is_approximate=true` / strict-excluded / `no_alpha_claim` honesty left intact; no data, code, or test counts changed (424/118 already current). Hosting prose stays deploy-coupled (committed `configured_not_observed` @ expected `16486e36…`; last live-proven `0f170441…`) and refreshes on the deploy, not here. No new **unowned** items surfaced — residual owned by CR-B21, regime un-degrade owned by CR-RDO-005. | — (docs only) | `docs/manual/`, `docs/FEATURES.md`, `docs/MANUAL_GENERATION_GUIDE.md`, `docs/review/index.html`; on-disk truth `data/vintage/raw/backfill-1990-01-01/_backfill_manifest.json` (`fail=0`, 6 FRED series `status=ok`) | No promotion: pure doc resync of an already-closed residual (see [[DOC-RECON-2026-06-16-CR-B21-RESIDUAL]]). Recorded for audit trail only. | Reopen only if a later doc pass finds a backfill/regime gap with no owning CR. |
| DOC-RECON-2026-06-17-H1 | Closed (no-op) | governance-evidence-resync | 2026-06-17 Epic H slice H-1 (deep-learning research lab) layered on the CR-RDO-005 dev baseline: added `quantlab/models/dl_forecaster.py`, `quantlab/models/dl/backends.py`, `quantlab/research/model_performance_report.py`, `quantlab/research/model_report_viz.py`, `scripts/run_dl_experiment.py`, 3 test files (22 tests), 3 mutations (`h-deep-report-oos-net-ranking`, `h-deep-report-no-alpha-claim`, `h-backend-honest-fallback`), and a second import-linter forbidden contract (engine/data ⊁ DL backend). Single-source counts moved on top of CR-RDO-005: pytest **402→424** (torch-enabled no-skip), **118/118** Python mutation spot checks (115/115→118/118), mypy →69 files, import-linter →87/237 (2 contracts), implemented epics 9→10. Regenerated the canonical dashboard payload as a deploy-gated re-pin at the time; public hosting was later re-proven on final `dataHash e5794260…` while the dashboard self-claim stays `not_proven`. All governance guards + 424 pytest green; H slice focused line coverage 95.7%. Everything `no_alpha_claim`. No new **unowned** items surfaced — owned by `h-deep-learning-research-lab`. | — (owned by H-1) | `quantlab/models/dl*`, `quantlab/research/model_*`, `scripts/run_dl_experiment.py`, `.agents/specs/{SPECS,NEXT_STEPS,RTM,TESTS,ISSUE_LOG}.md`, `quantlab/{TESTS,CORRECTNESS_CHECKLIST}.md`, `docs/{FEATURES,review,manual}` | No promotion: H-1 is an owned spec; deferred slices (real torch/jax/tf training, live UI, production Tier3) recorded in SPECS/NEXT_STEPS. Recorded for audit trail only. | Reopen only if a later H pass finds a gap with no owning spec/CR. |
| DOC-RECON-2026-06-16-CR-B21-RESIDUAL | Closed (no-op) | governance-evidence-resync | CR-B21 deep-backfill FRED rate/FX residual completed: an idempotent re-run of `scripts/backfill_history.py --since 1990-01-01` captured the 6 previously-throttled series (DGS10/DGS2/T10Y2Y/NASDAQCOM/DCOILWTICO/DEXTAUS) → manifest `ok=6 skip=18 fail=0`. `T10Y2Y` now available, so the **CR-RDO-005 regime family runs full-feature**; the committed multi-cycle artifact was regenerated (regime OOS-net Sharpe `0.6811→0.6694`, ranking unchanged, integration test green). Current-state surfaces (NEXT_STEPS/SPECS/RTM) + living CR docs reconciled. **Deploy-coupled stakeholder docs** (`docs/manual/*`, `docs/review/index.html`, `docs/FEATURES.md`, generation guides) still narrate the prior `18/24`/regime-degraded state and refresh on the deploy alongside the dashboard `dataHash` re-pin (ISSUE-RDO5-001). | — (data + docs) | `data/vintage/raw/backfill-1990-01-01/` (24 sources); `_backfill_manifest.json` `fail=0`; `real-data-oos-backtest/reports/multi-cycle-family-oos-artifact.json` | No promotion: residual owned by CR-B21, regime un-degrade owned by CR-RDO-005. Recorded for audit trail. | Reopen only if a later backfill or regime gap with no owning CR is found. |
| ISSUE-EVAL-001 | Resolved (folded into CR-DME-001) | model-eval-baseline-claim | `quantlab/models/evaluation.py::_claim_boundary` hard-raises unless `strategy_metadata.claim_boundary == "no_alpha_claim"`, but the canonical dumb baseline `BuyAndHold.metadata` omits the key — so `build_model_family_evaluation` cannot score a BuyAndHold baseline, the one row that must always stay visible in an OOS-net leaderboard. Surfaced while building CR-RDO-005 (which sidesteps it by rejecting only *explicit* overclaims and accepting a claim-silent baseline). | `d-model-family-evaluation` | `quantlab/models/evaluation.py:32-37`; `quantlab/strategies/buyandhold.py:26-27`; CR-RDO-005 `_assert_no_overclaim` workaround in `quantlab/research/multi_cycle_oos.py`. | Resolved by [CR-DME-001](./d-model-family-evaluation/change-requests/cr-dme-001-claim-silent-baseline.md): `_claim_boundary` now accepts a claim-silent dumb baseline and rejects only an explicit non-`no_alpha_claim` overclaim; `test_d_6` ranks a claim-silent `BuyAndHold` baseline; mutation `d-model-evaluation-alpha-gate` re-pointed + killed. | Closed — implemented as CR-DME-001 (Review PASSED). Reopen only if a regression reintroduces claim-silent rejection. |
| ISSUE-RDO5-001 | Resolved (public parity proven) | governance-evidence-resync | **Repo-side CR-RDO-005 work and deploy/public evidence are closed.** Mutation evidence is at **123/123** and the H-2 torch-enabled UAT captured **430 passed** with the optional torch lane running. Today’s default root verification remains labeled separately: `uv run pytest -q` → `434 passed, 2 skipped` (torch optional lanes skipped). After publishing `04b467a` to `main`, GitHub Pages built that commit (`pages/builds/1051287529`, status `built`) and the live public probe returned `proven` with HTTP 200 / manifest 200 / hash `matched` for `dataHash e5794260…`. | `real-data-oos-backtest` (CR-RDO-005) + `governance-evidence-refresh` | `real-data-oos-backtest/reports/cr-rdo-005-implementation-report.md`; `docs/showcase.json`; `docs/deployment-manifest.json`; `docs/public-hosting-probe.json`; `docs/review/assets/public-hosting-probe.json`; `npm run probe:public-demo` 2026-06-18T04:17Z → `proven`; Pages build `1051287529` for commit `04b467a`; current local gates 2026-06-18. | No new feature spec — owned by CR-RDO-005 plus resolved governance refresh. | Reopen only if public-hosting proof becomes stale/mismatched, or if a later dashboard payload refresh changes `dataHash` without a matching Pages proof refresh. |
| DOC-RECON-2026-06-15-DOCS | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-15 post-CR-B21 stakeholder-doc regeneration: bilingual manual (`docs/manual/{en,zh-tw}/index.{md,html}`) + executive review (`docs/review/index.html`) reconciled against `.agents/specs/{NEXT_STEPS,SPECS,ISSUE_LOG,RTM}.md` + `docs/FEATURES.md` + `**/review.md`. **Manual:** added Flow 6 (CR-B21 historical backfill + multi-cycle backtest + regime drawdown validation), updated Flow 5 to the 12-asset co-temporal run, refreshed `dataHash 0f170441`, live evidence re-captured (frontend smoke PASS, browser visual `16eff6f502544a718b4e403565b9d79b61e9461bb55494962e3cae59bf513504` 0-pixel, legacy FastAPI real curl). **Review:** CR-B21 added as a resolved gap + data-platform feature card, hosting alert/roadmap re-pinned to `proven`@`0f170441`, dates bumped to 2026-06-15. Generation guides (`MANUAL_GENERATION_GUIDE.md`, `REVIEW_GENERATION_GUIDE.md`) updated. Visual render-validation (headless chromium, 2026-06-15): manual en/zh + review render cleanly, no residual. Readiness copied from `review.md`; all 25 governance guards + 374 pytest green. No new **unowned** items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md`, `docs/FEATURES.md` | No promotion: every surfaced item traces to CR-B21 / CR-RDO-004 / CR-FPS-009/010/011. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| DOC-RECON-2026-06-15-B21 | Closed (no-op) | governance-evidence-resync | 2026-06-15 CR-B21 historical-backfill lane added `scripts/backfill_history.py` + `tests/test_backfill_history.py` (7 tests) + the `b-backfill-approximate-marking` mutation, moving the single-source governance counts pytest **367→374** and Python mutation **110/110→118/118**. Regenerated the canonical dashboard payload (gate transcript, `_FALLBACK_EVIDENCE_TESTS`, 4 synced showcase copies, static-export HTML mirror) → new `dataHash 0f170441…`; static visual baseline re-pinned (browser screenshot `16eff6f502544a718b4e403565b9d79b61e9461bb55494962e3cae59bf513504…` unchanged, 0-pixel diff, CR-FBP-001-legal); hosting probe/manifest were `configured_not_observed` / `mismatched` pre-deploy, then re-proven `proven` / `matched` after CR-B21 landed on `main` (`693f780`) and Pages redeployed (deployed==expected `0f170441…`, HTTP 200, observed `2026-06-15T07:07Z`); dashboard self-claim stays `not_proven` by design. All 25 governance guards + 374 pytest green. No new **unowned** items surfaced — the backfill is owned by CR-B21; the 6 throttled FRED series are an idempotently-completable residual recorded in `_backfill_manifest.json` and NEXT_STEPS. | — (owned by CR-B21) | `scripts/backfill_history.py`; `data/vintage/raw/backfill-1990-01-01/`; `docs/showcase.json`; `docs/deployment-manifest.json`; `.agents/specs/{NEXT_STEPS,SPECS,TESTS,RTM}.md`; `quantlab/{TESTS,CORRECTNESS_CHECKLIST}.md` | No promotion: count resync + backfill both traced to CR-B21. Recorded for audit trail only. | Reopen only if a later pass finds a backfill gap with no owning CR. |
| DOC-RECON-2026-06-14 | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-14 post-CR-RDO-004 manual + executive review regeneration, reconciled against `**/review.md`, `SPECS.md`, `NEXT_STEPS.md`, `RTM.md`, and `docs/FEATURES.md`. Current governance evidence: **424 passed**, 46 frontend tests, 0 frontend vulnerabilities, **123/123** Python mutation spot checks, 26/26 frontend mutations killed; dashboard `dataHash c73d7c88…`. Gaps resolved since last check: sampling-frequency honesty gap closed (CR-RDO-004 oversampling guard); public hosting re-proven (`status=proven`, deployed==expected `c73d7c88…`, HTTP 200, PR #97) while the dashboard self-claim stays `not_proven` by contract; real-data OOS-net demo live-captured (SP500 computed + CR-RDO-004 sampling-frequency/degeneracy fail-closed paths) and added as manual Flow 5. Front + back services then live-verified (Next.js smoke `PASS` served `/`+`/api/showcase`; legacy FastAPI real pyramid calc) and docs visually render-validated (headless chromium 1440×2400: manual+review render cleanly). The review UX-flow diagram was then inlined as a self-contained SVG (2026-06-15), removing the Mermaid CDN dependency so it renders offline / `file://`; no remaining visual residual. No new **unowned** improvement items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `.agents/specs/{NEXT_STEPS,SPECS,RTM}.md`, `docs/FEATURES.md` | No promotion: every resolved gap traces to CR-RDO-004 / CR-FBP-001 / CR-FPS-009/010/011; remaining open items (Stooq blocked, Tier3 deferred, approximate-PIT data) already owned. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| DOC-RECON-2026-06-12 | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-12/13 manual + executive review generation reconciled against `**/review.md`, `SPECS.md`, and refreshed gates. Current governance evidence is 424 passed, 44 frontend tests, 0 frontend vulnerabilities, 123/123 Python mutation spot checks configured, and 26/26 frontend mutations killed; browser-visual, public-hosting probe, deployment manifest/probe sync, stakeholder/app payload sync, review pytest/frontend-count/coverage/audit transcript freshness, stale F Next.js fixture/public/audit review wording, governance refresh review stale-evidence regression, local-first CI default and skill-body guards, import-linter count/formalization drift guards, E production identity URI-scheme guard, E production manifest artifact URI guard, E production experiment-binding guard, E production retraining artifact URI guard, E production artifact scheme allowlist guard, E production observed-at UTC guard, E production drift threshold guard, standalone probe parity-status/freshness/observedAt timestamp and age guards, non-self-staling promotion-boundary guard, autonomous cron dry-run proof, CR-B19 source-quorum proof, CR-FPS-006 canonical dashboard source proof, CR-FPS-007 stale-probe overclaim proof, CR-FPS-008 stale-observation proof, CR-FPS-009 visual-readiness wire-up, and occupied-3044 smoke-port chaos proof remain governed. No new **unowned** improvement items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `.agents/specs/RTM.md` | No promotion: every visual/data gap already traces to `ops-visual-drift-artifacts/review.md`, `f-*` reviews, CR-FPS-001..CR-FPS-009, CR-B19, or `ISSUE-B3-001`. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| ISSUE-B3-001 | Folded | data-source-contract-proof | 2026-06-11 live `scripts/daily_snapshot.py` run captured FRED/NOAA partial data but every configured Stooq symbol, including `2330.tw`, returned HTTP 404; configured FRED gold proxy also returned HTTP 404 and several FRED series timed out. | `b-data-platform` B-3 external source contract | `data/vintage/raw/2026-06-11/` contains `fred_FEDFUNDS`, `fred_CPIAUCSL`, `fred_GDPC1`, `fred_UNRATE`, `fred_SP500`, `noaa_oni`; command exited 1 with ok=6 fail=16. CR-B7 probe found `GOLDAMGBD228NLBM`/`GOLDPMGBD228NLBM` 404 and `PCOPPUSDM` 200. CR-B8 smoke found `yahoo:2330.TW` and `yahoo:^TWII` non-empty with `event_date=2026-06-11`. CR-B9 dry-run smoke listed no Stooq jobs and `fail=0`. CR-B12 scoped live smoke wrote `fred_FEDFUNDS` with `ok=1` and then skipped the existing same-day file with `skip=1`. CR-B19 proved the FRED/Yahoo/NOAA source quorum for 2026-06-12 while leaving Stooq blocked/default-disabled. CR-B20 added a dedicated Stooq proof wrapper; live `spy.us` probe on 2026-06-12 returned HTTP 404 and emitted `status=not_proven`. | Folded into [b-data-platform/change-requests/cr-b7-source-health.md](./b-data-platform/change-requests/cr-b7-source-health.md), [b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md](./b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md), [b-data-platform/change-requests/cr-b9-stooq-opt-in.md](./b-data-platform/change-requests/cr-b9-stooq-opt-in.md), [b-data-platform/change-requests/cr-b12-live-write-smoke.md](./b-data-platform/change-requests/cr-b12-live-write-smoke.md), [b-data-platform/change-requests/cr-b19-source-quorum-live-proof.md](./b-data-platform/change-requests/cr-b19-source-quorum-live-proof.md), and [b-data-platform/change-requests/cr-b20-stooq-contract-proof-wrapper.md](./b-data-platform/change-requests/cr-b20-stooq-contract-proof-wrapper.md). Stooq remains opt-in and external/source-contract blocked. | **WONT-DO — de-scoped 2026-06-15 (not worthy), do not reopen.** Live re-diagnosis: Stooq's `/q/l/` CSV endpoint returns HTTP 404 for all symbols (incl. `aapl.us`, so the endpoint was removed, not a symbol issue) and `/q/d/l/` is gated behind a JavaScript anti-bot challenge; Stooq has **no API key** (nothing to apply for). Permanently blocked/default-disabled — FRED/Yahoo/NOAA cover the proven quorum. `scripts/stooq_contract_proof.py` stays only as a fail-closed guard; restoring Stooq (headless JS-challenge bypass) is not worth the cost and is off the roadmap. |
