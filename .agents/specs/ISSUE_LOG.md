# ISSUE_LOG.md — Improvement Holding Surface

> Holds unresolved improvement items that are not yet safely closed by an active spec, CR, or review verdict. This is not a second `SPECS.md` and not a task dashboard.

| Issue ID | State | Cluster Key | Observation | Candidate Owner | Evidence | Routing Recommendation | Promotion Threshold |
|---|---|---|---|---|---|---|---|
| DOC-RECON-2026-06-16-CR-B21-RESIDUAL | Closed (no-op) | governance-evidence-resync | CR-B21 deep-backfill FRED rate/FX residual completed: an idempotent re-run of `scripts/backfill_history.py --since 1990-01-01` captured the 6 previously-throttled series (DGS10/DGS2/T10Y2Y/NASDAQCOM/DCOILWTICO/DEXTAUS) → manifest `ok=6 skip=18 fail=0`. `T10Y2Y` now available, so the **CR-RDO-005 regime family runs full-feature**; the committed multi-cycle artifact was regenerated (regime OOS-net Sharpe `0.6811→0.6694`, ranking unchanged, integration test green). Current-state surfaces (NEXT_STEPS/SPECS/RTM) + living CR docs reconciled. **Deploy-coupled stakeholder docs** (`docs/manual/*`, `docs/review/index.html`, `docs/FEATURES.md`, generation guides) still narrate the prior `18/24`/regime-degraded state and refresh on the deploy alongside the dashboard `dataHash` re-pin (ISSUE-RDO5-001). | — (data + docs) | `data/vintage/raw/backfill-1990-01-01/` (24 sources); `_backfill_manifest.json` `fail=0`; `real-data-oos-backtest/reports/multi-cycle-family-oos-artifact.json` | No promotion: residual owned by CR-B21, regime un-degrade owned by CR-RDO-005. Recorded for audit trail. | Reopen only if a later backfill or regime gap with no owning CR is found. |
| ISSUE-EVAL-001 | Resolved (folded into CR-DME-001) | model-eval-baseline-claim | `quantlab/models/evaluation.py::_claim_boundary` hard-raises unless `strategy_metadata.claim_boundary == "no_alpha_claim"`, but the canonical dumb baseline `BuyAndHold.metadata` omits the key — so `build_model_family_evaluation` cannot score a BuyAndHold baseline, the one row that must always stay visible in an OOS-net leaderboard. Surfaced while building CR-RDO-005 (which sidesteps it by rejecting only *explicit* overclaims and accepting a claim-silent baseline). | `d-model-family-evaluation` | `quantlab/models/evaluation.py:32-37`; `quantlab/strategies/buyandhold.py:26-27`; CR-RDO-005 `_assert_no_overclaim` workaround in `quantlab/research/multi_cycle_oos.py`. | Resolved by [CR-DME-001](./d-model-family-evaluation/change-requests/cr-dme-001-claim-silent-baseline.md): `_claim_boundary` now accepts a claim-silent dumb baseline and rejects only an explicit non-`no_alpha_claim` overclaim; `test_d_6` ranks a claim-silent `BuyAndHold` baseline; mutation `d-model-evaluation-alpha-gate` re-pointed + killed. | Closed — implemented as CR-DME-001 (Review PASSED). Reopen only if a regression reintroduces claim-silent rejection. |
| ISSUE-RDO5-001 | Mostly resolved — dashboard re-pin deploy-gated (owned by CR-RDO-005) | governance-evidence-resync | **Mutation + pytest done:** the full Python mutation suite was regenerated end-to-end to **115/115** (`gate-python-mutation.json` + 6 sync surfaces synced); doing so required repairing **5 pre-existing stale hosting-mutation anchors** (`public-hosting-{manifest,probe}-{status,hash}-overclaim` + review-probe copy) pinned to `configured_not_observed`/`mismatched` from before the CR-B21 deploy re-pinned the committed manifest/probe to `proven`/`matched` — a latent breakage that had silently blocked any end-to-end mutation re-run (the committed 111 JSON predated it). **Pytest done:** the canonical no-skip pytest gate was captured by transiently installing torch into the venv (PyTorch LSTM lane runs, no skip) → `gate-pytest.txt` **402 passed**; the review-gate count surfaces (TESTS.md, both generation guides, `docs/review/index.html`, manuals, registry) are synced to 402. Confirms the pre-existing drift: surfaces now publish `402 passed` (torch-enabled, no-skip) while the default torch-excluded env yields 398 + the optional lane skipped. **Remaining (deploy-gated only):** the committed dashboard payload (`docs/**/showcase.json`, `dataHash 0f170441…`) + manifest/probe + browser-visual VRT re-pin still lag at 374/111 — regenerating them flips committed hosting `proven`→`configured_not_observed` until Pages serves the new `dataHash`, so that re-pin is intrinsically deploy-coupled (mirrors CR-RDO-004) and rides the deploy refresh. | `real-data-oos-backtest` (CR-RDO-005) | `real-data-oos-backtest/reports/cr-rdo-005-implementation-report.md`; `NEXT_STEPS.md` Recommended Next Action #6; mutation suite repaired in `scripts/run_mutation_spot_checks.py`; `gate-python-mutation.json` now `total=115`. | No promotion — owned by CR-RDO-005; the mutation half is closed, the pytest/dashboard half closes on the torch-enabled UAT + deploy refresh. | Closes fully on the UAT-capture pytest refresh + deploy-gated dashboard re-pin; reopen only if a gap with no owning CR is found afterward. |
| DOC-RECON-2026-06-15-DOCS | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-15 post-CR-B21 stakeholder-doc regeneration: bilingual manual (`docs/manual/{en,zh-tw}/index.{md,html}`) + executive review (`docs/review/index.html`) reconciled against `.agents/specs/{NEXT_STEPS,SPECS,ISSUE_LOG,RTM}.md` + `docs/FEATURES.md` + `**/review.md`. **Manual:** added Flow 6 (CR-B21 historical backfill + multi-cycle backtest + regime drawdown validation), updated Flow 5 to the 12-asset co-temporal run, refreshed `dataHash 0f170441`, live evidence re-captured (frontend smoke PASS, browser visual `ddd8c9a5` 0-pixel, legacy FastAPI real curl). **Review:** CR-B21 added as a resolved gap + data-platform feature card, hosting alert/roadmap re-pinned to `proven`@`0f170441`, dates bumped to 2026-06-15. Generation guides (`MANUAL_GENERATION_GUIDE.md`, `REVIEW_GENERATION_GUIDE.md`) updated. Visual render-validation (headless chromium, 2026-06-15): manual en/zh + review render cleanly, no residual. Readiness copied from `review.md`; all 25 governance guards + 374 pytest green. No new **unowned** items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `docs/{MANUAL,REVIEW}_GENERATION_GUIDE.md`, `docs/FEATURES.md` | No promotion: every surfaced item traces to CR-B21 / CR-RDO-004 / CR-FPS-009/010/011. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| DOC-RECON-2026-06-15-B21 | Closed (no-op) | governance-evidence-resync | 2026-06-15 CR-B21 historical-backfill lane added `scripts/backfill_history.py` + `tests/test_backfill_history.py` (7 tests) + the `b-backfill-approximate-marking` mutation, moving the single-source governance counts pytest **367→374** and Python mutation **110/110→111/111**. Regenerated the canonical dashboard payload (gate transcript, `_FALLBACK_EVIDENCE_TESTS`, 4 synced showcase copies, static-export HTML mirror) → new `dataHash 0f170441…`; static visual baseline re-pinned (browser screenshot `ddd8c9a5…` unchanged, 0-pixel diff, CR-FBP-001-legal); hosting probe/manifest were `configured_not_observed` / `mismatched` pre-deploy, then re-proven `proven` / `matched` after CR-B21 landed on `main` (`693f780`) and Pages redeployed (deployed==expected `0f170441…`, HTTP 200, observed `2026-06-15T07:07Z`); dashboard self-claim stays `not_proven` by design. All 25 governance guards + 374 pytest green. No new **unowned** items surfaced — the backfill is owned by CR-B21; the 6 throttled FRED series are an idempotently-completable residual recorded in `_backfill_manifest.json` and NEXT_STEPS. | — (owned by CR-B21) | `scripts/backfill_history.py`; `data/vintage/raw/backfill-1990-01-01/`; `docs/showcase.json`; `docs/deployment-manifest.json`; `.agents/specs/{NEXT_STEPS,SPECS,TESTS,RTM}.md`; `quantlab/{TESTS,CORRECTNESS_CHECKLIST}.md` | No promotion: count resync + backfill both traced to CR-B21. Recorded for audit trail only. | Reopen only if a later pass finds a backfill gap with no owning CR. |
| DOC-RECON-2026-06-14 | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-14 post-CR-RDO-004 manual + executive review regeneration, reconciled against `**/review.md`, `SPECS.md`, `NEXT_STEPS.md`, `RTM.md`, and `docs/FEATURES.md`. Current governance evidence: **402 passed**, 46 frontend tests, 0 frontend vulnerabilities, **115/115** Python mutation spot checks, 26/26 frontend mutations killed; dashboard `dataHash c73d7c88…`. Gaps resolved since last check: sampling-frequency honesty gap closed (CR-RDO-004 oversampling guard); public hosting re-proven (`status=proven`, deployed==expected `c73d7c88…`, HTTP 200, PR #97) while the dashboard self-claim stays `not_proven` by contract; real-data OOS-net demo live-captured (SP500 computed + CR-RDO-004 sampling-frequency/degeneracy fail-closed paths) and added as manual Flow 5. Front + back services then live-verified (Next.js smoke `PASS` served `/`+`/api/showcase`; legacy FastAPI real pyramid calc) and docs visually render-validated (headless chromium 1440×2400: manual+review render cleanly). The review UX-flow diagram was then inlined as a self-contained SVG (2026-06-15), removing the Mermaid CDN dependency so it renders offline / `file://`; no remaining visual residual. No new **unowned** improvement items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `.agents/specs/{NEXT_STEPS,SPECS,RTM}.md`, `docs/FEATURES.md` | No promotion: every resolved gap traces to CR-RDO-004 / CR-FBP-001 / CR-FPS-009/010/011; remaining open items (Stooq blocked, Tier3 deferred, approximate-PIT data) already owned. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| DOC-RECON-2026-06-12 | Closed (no-op) | stakeholder-doc-reconciliation | 2026-06-12/13 manual + executive review generation reconciled against `**/review.md`, `SPECS.md`, and refreshed gates. Current governance evidence is 402 passed, 44 frontend tests, 0 frontend vulnerabilities, 115/115 Python mutation spot checks configured, and 26/26 frontend mutations killed; browser-visual, public-hosting probe, deployment manifest/probe sync, stakeholder/app payload sync, review pytest/frontend-count/coverage/audit transcript freshness, stale F Next.js fixture/public/audit review wording, governance refresh review stale-evidence regression, local-first CI default and skill-body guards, import-linter count/formalization drift guards, E production identity URI-scheme guard, E production manifest artifact URI guard, E production experiment-binding guard, E production retraining artifact URI guard, E production artifact scheme allowlist guard, E production observed-at UTC guard, E production drift threshold guard, standalone probe parity-status/freshness/observedAt timestamp and age guards, non-self-staling promotion-boundary guard, autonomous cron dry-run proof, CR-B19 source-quorum proof, CR-FPS-006 canonical dashboard source proof, CR-FPS-007 stale-probe overclaim proof, CR-FPS-008 stale-observation proof, CR-FPS-009 visual-readiness wire-up, and occupied-3044 smoke-port chaos proof remain governed. No new **unowned** improvement items surfaced. | — (docs only) | `docs/manual/`, `docs/review/`, `.agents/specs/RTM.md` | No promotion: every visual/data gap already traces to `ops-visual-drift-artifacts/review.md`, `f-*` reviews, CR-FPS-001..CR-FPS-009, CR-B19, or `ISSUE-B3-001`. Recorded for audit trail only. | Reopen only if a future doc pass finds a gap with no owning spec/CR/review. |
| ISSUE-B3-001 | Folded | data-source-contract-proof | 2026-06-11 live `scripts/daily_snapshot.py` run captured FRED/NOAA partial data but every configured Stooq symbol, including `2330.tw`, returned HTTP 404; configured FRED gold proxy also returned HTTP 404 and several FRED series timed out. | `b-data-platform` B-3 external source contract | `data/vintage/raw/2026-06-11/` contains `fred_FEDFUNDS`, `fred_CPIAUCSL`, `fred_GDPC1`, `fred_UNRATE`, `fred_SP500`, `noaa_oni`; command exited 1 with ok=6 fail=16. CR-B7 probe found `GOLDAMGBD228NLBM`/`GOLDPMGBD228NLBM` 404 and `PCOPPUSDM` 200. CR-B8 smoke found `yahoo:2330.TW` and `yahoo:^TWII` non-empty with `event_date=2026-06-11`. CR-B9 dry-run smoke listed no Stooq jobs and `fail=0`. CR-B12 scoped live smoke wrote `fred_FEDFUNDS` with `ok=1` and then skipped the existing same-day file with `skip=1`. CR-B19 proved the FRED/Yahoo/NOAA source quorum for 2026-06-12 while leaving Stooq blocked/default-disabled. CR-B20 added a dedicated Stooq proof wrapper; live `spy.us` probe on 2026-06-12 returned HTTP 404 and emitted `status=not_proven`. | Folded into [b-data-platform/change-requests/cr-b7-source-health.md](./b-data-platform/change-requests/cr-b7-source-health.md), [b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md](./b-data-platform/change-requests/cr-b8-yahoo-chart-fallback.md), [b-data-platform/change-requests/cr-b9-stooq-opt-in.md](./b-data-platform/change-requests/cr-b9-stooq-opt-in.md), [b-data-platform/change-requests/cr-b12-live-write-smoke.md](./b-data-platform/change-requests/cr-b12-live-write-smoke.md), [b-data-platform/change-requests/cr-b19-source-quorum-live-proof.md](./b-data-platform/change-requests/cr-b19-source-quorum-live-proof.md), and [b-data-platform/change-requests/cr-b20-stooq-contract-proof-wrapper.md](./b-data-platform/change-requests/cr-b20-stooq-contract-proof-wrapper.md). Stooq remains opt-in and external/source-contract blocked. | **WONT-DO — de-scoped 2026-06-15 (not worthy), do not reopen.** Live re-diagnosis: Stooq's `/q/l/` CSV endpoint returns HTTP 404 for all symbols (incl. `aapl.us`, so the endpoint was removed, not a symbol issue) and `/q/d/l/` is gated behind a JavaScript anti-bot challenge; Stooq has **no API key** (nothing to apply for). Permanently blocked/default-disabled — FRED/Yahoo/NOAA cover the proven quorum. `scripts/stooq_contract_proof.py` stays only as a fail-closed guard; restoring Stooq (headless JS-challenge bypass) is not worth the cost and is off the roadmap. |
